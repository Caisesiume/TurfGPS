# TurfGPS requirements

The front door to the requirements corpus. Every functional and non-functional requirement lives in exactly one category file in this folder, in the canonical record form defined by `requirements-authoring`. This page holds the three things that change rarely and that everything else in the folder depends on: the **category register**, the **ID allocation ledger**, and the folder's **invariants**.

Why a folder rather than a file: `docs/README.md § Requirements/`.

## Corpus state

**Corpus state — 6 August 2026.** 42 requirements, 10 categories in use. **39 are `to-build` and 3 are `draft`.** The three are batch 3b — `FR-035` … `FR-037` — filed on 6 August 2026 and **awaiting the Owner's sign-off**. All three are `Turf data integration`; no category was added for them and none was needed. `draft` stood empty from 4 August until they landed, which is the ordinary interval between a sign-off and the next filing rather than a state to repair.

**Batch 3's sign-off, 4 August 2026.** The twenty-one records `FR-019` … `FR-034` and `NFR-001` … `NFR-005` were **signed off by the Owner on 4 August 2026**; nineteen were filed on 3 August 2026 and `FR-033` … `FR-034` on 4 August, added to the same batch on a ruling that closed one of its own open questions. They moved from `draft` **straight to `to-build`**, as both earlier batches did, because `@requirements-reconciler` is dormant and there is no classification step for a record to wait in `approved` for — no record in this corpus has ever held `approved`.

**Batch 3 was the first batch to sit in `draft` after a filing pass**, which was an ordinary state rather than a stall — it had simply never been observed here, because batches 1 and 2 were each signed off the day they were filed. `FR-001` … `FR-012` were signed off by the Owner on 1 August 2026 and `FR-013` … `FR-018` on 2 August 2026. Story creation has run for every **signed-off** requirement in the corpus: `@requirements-story-organizer` filed four epics and seventeen stories on 2 August 2026, in two passes, one per sign-off, and five further epics and nineteen stories on 4 August 2026 for batch 3. The links live in `TRACEABILITY.md`; each record's `Resolved-by` and the column in `INDEX.md` are views of them.

**Batch 3b sat in `draft` after its filing pass, and story creation was deliberately not run for it.** Sign-off gates story creation, not the reverse, so `FR-035` … `FR-037` carry no epic and no story and will carry none until the Owner signs. This is the same shape batch 3 held between 3 and 4 August and is recorded again only because the corpus has spent most of its life with the two in step.

**39 records name at least one story in `Resolved-by`; the 3 filed by batch 3b read `—`** — nine epics and thirty-six stories carry the 39. Those three are the first `—` in the corpus since story creation caught up with filing on 4 August 2026, and the em-dash carries its one meaning: *not yet allocated*, never *unknown*. None is an orphan either, because the orphan test applies only to a requirement whose epic is in flight, and no epic has been cut for them.

## The files in this folder

| File | Holds |
|---|---|
| `README.md` | This page: front door, category register, ID allocation ledger, folder invariants |
| `INDEX.md` | One index table per category, carrying a row for every ID ever issued |
| `journey-definition.md` | The `Journey definition` records — one file per category, named from the register below |
| `turf-data-integration.md` | The `Turf data integration` records |
| `zone-data-fidelity.md` | The `Zone data fidelity` records |
| `cost-and-time-composition.md` | The `Cost and time composition` records |
| `value-model.md` | The `Value model` records |
| `objective-selection-and-ranking.md` | The `Objective selection and ranking` records |
| `recommendation-set-composition.md` | The `Recommendation set composition` records |
| `data-currency-and-confidence.md` | The `Data currency and confidence` records |
| `outbound-rate-compliance.md` | The `Outbound rate compliance` records |
| `runtime-and-deployment-shape.md` | The `Runtime and deployment shape` records |
| `TRACEABILITY.md` | The requirement ↔ epic ↔ story matrix, both directions |

**Why the index is a separate file.** The allocation ledger is the only guard against ID reuse, and it must not sit below fifteen screens of index table where an author stops scrolling. The register and the ledger change rarely; the index changes on every filing. Keeping them apart means a routine filing does not rewrite the one file the whole RE family reads first, and two agents filing different categories do not serialize against a single file.

## Category register

The corpus's controlled `Category` vocabulary, and its only home. A `Category` value is legal if and only if it appears in the **Category** column below, copied verbatim. `@requirements-engineer` alone adds a name here — a new category is a decision, never a side effect of authoring. `@requirements-librarian` files each record under its `Category` and raises a record carrying a name not on this list as a finding, rather than filing it under a near-match.

**A name on this list is permission to file, not a promise that requirements exist.** Most rows read `reserved`: the source sections behind them have not been broken down yet. `reserved` means no file and no records, and nothing may be concluded from the absence — a category's absence from the *filed* set means unfiled, never illegal.

These names were consolidated on 1 August 2026 from coverage prompts that previously stood in four agent definitions, which named the same areas in four different phrasings and would have produced several files for one subsystem. Those lists remain in place as coverage prompts — questions an author asks per source section — and are explicitly no longer a source of category names.

**Extensions since.** `Value model`, added 1 August 2026 by `@requirements-engineer` while integrating `SPECIFICATION.md § The journey as an optimization problem`. The register as consolidated carried `Cost and time composition` with no counterpart on the value side, while the objective the whole product optimizes is value set against cost. Without it, everything the value half of that objective is built from — zone attributes, attribute rarity and preference, the rank-to-weight curve — would have filed under `Objective selection and ranking`, whose scope is how value and cost are *combined*, not what either is made of. Recorded here because a register extension is a decision, and a decision with no stated reason is indistinguishable later from a name someone coined in passing.

`Recommendation set composition`, added 2 August 2026 by `@requirements-engineer` while integrating `SPECIFICATION.md § Recommended journey alternatives`. The register carried `Route alternative generation` for producing corridors and `Objective selection and ranking` for the objective and the ordering under it, and nothing for the question that section actually answers: which of the alternatives produced are put in front of the user. Filing those records under `Objective selection and ranking` would have put set membership and set ordering in one file under a scope line that names only the second, and coverage per subsystem would have stopped being checkable for either. Recorded here because a register extension is a decision, and a decision with no stated reason is indistinguishable later from a name someone coined in passing.

`Turf data integration`, added 3 August 2026 by `@requirements-engineer` while integrating batch 3. The register as consolidated was seeded from the decision core and carried no category for the data plane at all — the layer between the Turf API and everything downstream of it. Batch 3 is the first batch to enter that layer. Filing these records under the decision-core categories would have put the integration contract in the same files as the optimizer's behaviour, and coverage per subsystem would have stopped being checkable for both. The boundary against `Call budget` is written into the scope line rather than left to be rediscovered, because the two abut and the line is not obvious.

`Zone data fidelity`, added 3 August 2026 by `@requirements-engineer` while integrating batch 3. Separated from `Turf data integration` deliberately. Its records bind the optimizer and the cost model rather than the integration layer, and folding them in would mean a reviewer asking whether the cost model assumes a minimum inter-zone distance has to read the integration file. It is small — three records — and the register already carries a category of one; size is not the test, subsystem is.

`Outbound rate compliance` and `Data currency and confidence`, added 3 August 2026 by `@requirements-engineer` on the Owner's ruling of the same day: `Turf data integration` splits by record kind, rather than the skill's one-kind-per-file rule bending to admit a category holding both. They are the NFR half of what batch 3 filed as a single `FR + NFR` category, and they close the open finding recorded under *How this folder works* below.

**The split is by subsystem too, not by kind alone, and that is why honest names exist for it.** A doubled register — one name per subsystem on the FR side and the same name with a word appended on the NFR side — would have made `Category` a restatement of `Kind` and would have said nothing about the system. It was avoidable because the two records were never held together by their subsystem in the first place: they were held together by their *source section*, and the FR file had already drawn the line between them in prose. `FR-024`'s `Rationale` reads *what the system may conclude from a copy of that age is a confidence question, and it is `NFR-002`'s* — the boundary between what the pipeline tolerates and what it may claim, written by an author with no idea it would become a filing boundary. The register now states that line instead of leaving it in one record's rationale.

**The NFR half of this register was never a subsystem vocabulary**, which is the deeper reason no doubling occurred. `Response time`, `Estimate accuracy`, `Call budget`, `Privacy`, `Portability`, `Observability` and `Maintainability` each name a **quality**, where every FR name names a **subsystem**. One column has carried two vocabularies since consolidation and `Kind` is what tells them apart — so `Turf data integration`, registered as `FR + NFR`, was the sole row spanning both, and the ruling's real effect is to remove the one exception rather than to introduce a division. Neither new name is a Turf-integration name with a qualifier: one names a compliance obligation at the egress edge and would hold a rate record for any limited upstream, the other names an age-to-confidence relation and would hold one for any ageing source.

`Runtime and deployment shape`, added 3 August 2026 by `@requirements-engineer` while integrating batch 3. Added to carry the NFRs that repository scaffolding satisfies, following the Owner's ruling of 3 August 2026 that `DELIVERY.md` carries **no traceability exception for scaffolding** and that scaffolding must be backed by real requirements. No existing category fitted: `Portability` is about adding a country as an adapter, `Maintainability` about the cost of the next safe change, and `Platform support` about the browsers the client must work in. What the build produces, and which deployment topologies the architecture rules out, had no home.

| Category | Kind | File | Scope | State |
|---|---|---|---|---|
| Journey definition | FR | `journey-definition.md` | The destinations a request must and may name, the order they are visited in, their preservation through optimization, and the mode of travel between them | **in use** |
| Journey initialization | FR | — | How a journey is entered and a planning session begins: input capture, the time budget, validation, session start. **What a journey must and may name is `Journey definition`, not this** — the split is the act of entering against the thing entered | reserved |
| Route alternative generation | FR | — | Producing the candidate corridors a journey may take | reserved |
| Turf data integration | FR | `turf-data-integration.md` | How zone, player and region data is obtained from the Turf API and what may be relied on from it: which source answers which question, the local synced copy and the staleness the pipeline tolerates in it, the shape constraints the endpoints place on a single request, and the fields whose absence or round scope constrains what can be concluded. Two neighbours abut it and neither is folded in: **rate over time against the API is `Outbound rate compliance`; per-journey call volume is `Call budget`** — this category owns what is fetched and what it means, never how often it may be asked for | **in use** |
| Zone data fidelity | FR | `zone-data-fidelity.md` | What the system may and may not assume about an individual zone, and about the relationship between two zones, given what the Turf API exposes and what it merely *guides*. Distinct from `Turf data integration`, which owns how that data is obtained and kept current — this category owns the limits the data's nature places on everything downstream of it, including the cost model and the optimizer | **in use** |
| Candidate identification | FR | — | Selecting which zones enter evaluation, and bounding that set | reserved |
| Access classification | FR | — | Deciding whether and how a zone can be reached and stopped at | reserved |
| Cost and time composition | FR | `cost-and-time-composition.md` | Composing stop cost and journey cost from routing, walking and manoeuvre components | **in use** |
| Value model | FR | `value-model.md` | What a journey's Turf value is made of: the zones counted toward it, the attributes that weight them, and the user's stated preferences over those attributes. The mirror of `Cost and time composition`; the two are combined by `Objective selection and ranking`, which owns neither | **in use** |
| Objective selection and ranking | FR | `objective-selection-and-ranking.md` | The objective a plan optimizes, and the ordering of alternatives under it | **in use** |
| Recommendation set composition | FR | `recommendation-set-composition.md` | Which journey alternatives reach the user and what the offered set must hold: how many, how varied, what is dropped as redundant or dominated, and what may never be withheld. Distinct from `Route alternative generation`, which produces the corridors, and from `Objective selection and ranking`, which decides the objective and the order — this category decides membership of the offered set, not its ranking | **in use** |
| Review and replacement | FR | — | The user's review loop: rejecting a zone and receiving a replacement | reserved |
| Hand-off and dispatch | FR | — | Delivering a chosen plan to the device and the navigation app that will drive it | reserved |
| Persistence and staleness | FR | — | Storing a plan, retrieving it, and what makes a stored plan no longer trustworthy | reserved |
| Safety exclusions | FR | — | The enforceable exclusions and the absolute ceiling, as behaviour the system must exhibit | reserved |
| Response time | NFR | — | How long the initial solve and the review-time replacement may take | reserved |
| Estimate accuracy | NFR | — | Calibration of presented estimates, and the width of the ranges carrying them | reserved |
| Coverage and data quality | NFR | — | Graceful degradation where map or elevation data is thin, and confidence falling with it | reserved |
| Data currency and confidence | NFR | `data-currency-and-confidence.md` | How the age of the data a recommendation was built from bounds the confidence the system may record for it. Its trigger is the **age** of data that is otherwise complete, which is what separates it from `Coverage and data quality`, whose trigger is data that is **thin**; and its subject is the confidence carried by the recommendation rather than the calibration of the time estimate inside it, which is what separates it from `Estimate accuracy` | **in use** |
| Classification correctness | NFR | — | The product's stated measure of success: no zone classified confidently and wrongly | reserved |
| Call budget | NFR | — | Bounded and known per-journey external call volume | reserved |
| Outbound rate compliance | NFR | `outbound-rate-compliance.md` | Holding the system's outbound request rate within the limits an external service publishes, counted across every caller inside the system at once — solve sessions and background jobs together, since the limit is the other party's and applies to the system as a whole. Distinct from `Call budget`, which bounds the per-journey call **volume** the system chooses for itself: this category answers to a ceiling set elsewhere, measured per unit time, and owns the aggregation point at which it is honoured | **in use** |
| Privacy | NFR | — | What a stored plan holds, and what the system declines to hold at all | reserved |
| Portability | NFR | — | Adding a country as an adapter rather than a rewrite | reserved |
| Platform support | NFR | — | Mobile-first web: desktop and mobile, iOS and Android browsers | reserved |
| Runtime and deployment shape | NFR | `runtime-and-deployment-shape.md` | The form the system is built and run in: what the build produces, how the client is served, the statefulness the solve session imposes, and the deployment topologies the architecture rules out. Distinct from `Platform support`, which is about the browsers the client must work in | **in use** |
| Observability | NFR | — | What must be measurable about a running system, including the decisions the design earmarks for measurement | reserved |
| Maintainability | NFR | — | The cost of the next safe change, including the one-home-per-model invariant | reserved |

A category file is created the first time a record is filed under its name, and the **File** column changes from `—` to the filename in the same pass.

## ID allocation ledger

**Next free: FR-038 · NFR-006**

**Allocation is highest-ever-issued plus one, never lowest-unused**, so a retirement cannot free a number even if a tombstone is lost. One ascending sequence per prefix, the two counters running independently.

**`FR-000` and `NFR-000` are permanently reserved and are never allocated.** They carry the worked examples in `requirements-authoring`, which has to illustrate the record form without any illustration answering to a live requirement's identity. The reservation costs nothing: both sequences run upward from `001`, so neither number is reachable by the rule above. Until 3 August 2026 the skill's canonical example was numbered `FR-021`, which batch 3 then issued to a real record — `grep FR-021` returned one real requirement and one fictional one, and `FR-032`'s `Rationale` cites `FR-021` by number, so it resolved to either. **The example was renumbered and the record was not**, which is the only order the immutability rule permits: an allocated ID is immutable, and the example's number was never an allocation.

**The NFR sequence opened on 3 August 2026, with `NFR-001` … `NFR-005`.** Until batch 3 no NFR had ever been issued, and the emptiness was a recorded outcome rather than a gap: `@requirements-nfr` returned a considered zero for batch 1, scoped to `SPECIFICATION.md § Primary use case` and its subsection *The planning player* — neither states a quality attribute with an obligation attached — a second considered zero, argued and accepted, for `SPECIFICATION.md § The journey as an optimization problem`, the section behind `FR-007` … `FR-011`, and a third for `SPECIFICATION.md § Recommended journey alternatives`, the section behind `FR-013` … `FR-018`, argued at the foot of this ledger. **All three remain correct in their own scope and none is overturned by this batch.** They are still the reason `NFR-001` was free until now, and they are kept in full below for that reason.

The drought ended where those zeros did not reach. Batch 3's NFRs came from `Architecture.md § Data sources and constraints` and `Architecture.md § Technology decisions` — sections none of the three zeros covered, and neither of them a section a zero was ever put to. Nothing about the sequence opening here says a zero was wrong; it says a different document was swept.

**Batch 3's de-confliction, recorded so it is not re-litigated.** Four authored records did not file, and an ID was issued for none of them. The FR and NFR lanes swept the same Architecture sections in parallel, so the overlaps below are the ordinary product of that and not a fault in either lane.

- **A functional record on Turf rate limits was dropped in favour of `NFR-001`.** Both lanes authored the same obligation and both chose `test`. A bound on a rate is a quality, and `NFR-001` alone names the aggregation point — a limiter held per session or per worker satisfies a per-caller criterion and still breaches a limit the API applies to the system as a whole.
- **A non-functional record on solving against a refreshing or stale copy was dropped in favour of `FR-022` and `FR-024`.** It duplicated `FR-024` nearly word for word, and its one distinct clause — that a solve does not wait on the sync — is already `FR-022`'s second criterion.
- **A non-functional record forbidding an assumed minimum inter-zone distance was ruled functional** and re-authored as `FR-032`. A prohibition on a behaviour is not a quality attribute; an acceptance criterion of the form *count of X: zero* would make every prohibition an NFR. The content survived because `FR-021` genuinely does not reach logic that obtains no distance at all.
- **A fourth considered zero, on `Architecture.md § API version`, is accepted in its own lane** — the NFR lane returned it, and there is no quality attribute in that section. `FR-019` stands over it because it carries what neither Architecture section does: that **exactly one** API version is named, on error and fallback paths as well as primary ones.

**Amended after filing, 3 August 2026.** Recorded because a field changed after the batch was filed, and a diff alone would not say why. The first two follow the Owner's rulings of that day; the third is a conformance reject raised by the sweep those rulings forced.

- **`FR-025` drops from `SHOULD` to `COULD`.** `Architecture.md § Retrieving zones` makes the local synced copy the source for corridor resolution, while `Architecture.md § Volatile and optional fields` calls any locally cached copy a snapshot; the Owner ruled that the sync is the source and that no per-journey refresh is required for the first release, which puts the bounding-box endpoint — the only endpoint `FR-025` constrains — off the first-release critical path. The supporting measurement is `@data-architect`'s: **1.28% of zones change `totalTakeovers` per hour** and 23,119 of them, or 16.6%, per day, against a sync feeding data that is either near-static or rated over months. A per-journey refresh buys nothing measurable and adds a live API dependency inside every solve. **`COULD` and not `WON'T-now`**: the endpoint is not excluded — the same section keeps it as the fallback and as the means of refreshing volatile per-zone fields — so the obligation binds the moment that path is built and the statement stays `shall`. `WON'T-now` would file no story at all and leave a fallback free to construct requests the API rejects.
- **`NFR-001`'s second acceptance criterion now names the all-zones endpoint by role rather than by path.** It read `GET /v5/zones/all`, which made the record a second home for the API version — a constant whose home is `Architecture.md § API version`, and which `FR-019` exists to bind by citation, its `Rationale` saying exactly that. Raised by `@requirements-librarian` during the re-file sweep and rewritten by `@requirements-nfr`. **The reject is `Verifiable`, not merely `Conforming`**, which is why it was a rewrite and not a note: on a version bump the criterion would still have *passed*, vacuously, because a deployment on the next version issues no `/v5/` requests at all and the interval between zero requests satisfies any minimum. A criterion that reports green while measuring nothing is the one kind that can never fail. A `/v[0-9]/` sweep of the corpus returned that line and no other, and `FR-022` had already named the same endpoint version-free, so the record was the outlier against its own batch. Caught in `draft`: no story, no index row and no signed record moved with it.
- **Nothing is amended for `dateCreated`, and nothing needed to be.** Whether the live API still returns the field is being verified against the endpoint on the Owner's ruling of the same day; the January 2025 dump carries it, which establishes the dump rather than today. **No record in this corpus names `dateCreated`** — the field appears only in the upstream documents, under `CalculationSpecification.md § Takeover rate` and the sections citing it — so nothing filed rests on the answer and nothing was held back pending it. Recorded so that the batch which eventually reaches takeover rate finds the verification rather than re-deriving the question. **Both halves are closed by batch 3b, 6 August 2026, and the note did its job.** `ff03613` answers the verification — `dateCreated` is on 100.00% of records across the complete response, counted rather than sampled — and `FR-035` makes the second sentence false on filing, being the first record in this corpus to name the field. Left standing rather than deleted, because what it recorded is why the question was not re-derived.

**Added to batch 3 after filing, 4 August 2026.** `FR-033` and `FR-034`, on the Owner's ruling of 3 August 2026 closing the batch's open third question: plan against whatever copy of the zone set exists and mark the result as built from stale data, refusing only where no copy exists at all. This is authoring rather than amendment — the obligation belongs to `Architecture.md § Retrieving zones`, a section batch 3 swept, and the batch was still `draft` when they were written, so no signed record moved and no story was disturbed. **The ruling produced two records rather than the one commissioned**, and the split is recorded here because a one-record commission returning two reads as drift unless the reason is written down. Planning past the bound and recording what the plan was built from are two separately testable outcomes on the **same** branch, so a statement joining them would be half-satisfiable by the half that keeps the product working — which is the half nobody forgets to build. Planning and refusing, by contrast, are two branches of one gate reading one input, whether a copy is held, and they stay together in `FR-033` exactly as `FR-012`'s three criteria do. Both are `MUST`: the ruling permits degradation **on condition** that it is disclosed, and a split made for singularity must not quietly demote the condition below the behaviour it conditions.

**No register extension was needed for either, and one was considered.** `FR-034`'s subject is the result rather than the data plane, which argued for a category of its own; it was rejected. `Data currency and confidence` is registered `NFR` and cannot hold a functional record. A new functional category for one record would put the marking rule in a different file from the planning rule it conditions, so a reviewer asking what happens past the bound would read two files for one ruling, and it would pre-empt `Persistence and staleness`, which owns whether that fact survives storage and retrieval. The deciding line is one the register has already drawn: `FR-024`'s rationale puts what the system may **conclude** from a copy of that age on the confidence side, with `NFR-002`. `FR-034` concludes nothing — it records a fact about provenance — so it sits on the pipeline side of a line already drawn, and needs no new name.

**Repaired before sign-off, 4 August 2026 — two records, one failure mode.** Both are sources that moved under a filed record, which is not an authoring error and is exactly what requirements management exists to catch. Neither record was wrong when written; both were wrong when read, and nothing in either record could have told a reader so. Recorded together because the pair is the argument: a corpus is not made correct by careful authoring alone, and the interval in which a record can silently rot is as short as the second case shows.

- **`FR-022`'s `Risk` loses its first clause.** It rested the obligation on an all-zones download "slow enough to have timed out on a partial transfer" — an observation `Architecture.md § Retrieving zones` has since retracted as no longer reproducing. **Batch 3 was filed at 12:27:11 on 3 August 2026 and `ff03613` landed at 12:27:48**, so the clause was true when written and false thirty-seven seconds later, with no edit to the record and nothing in it to signal the change. The obligation itself never depended on the retracted half: the same `Risk` already named the rate limit, and the architecture's own conclusion is that freshness is bounded by that limit however fast the transfer is. The repair drops the timeout, keeps the limit, and adds the guard the incident earned — a fast download is no reason to move the sync onto the request path — so the stronger-sounding argument is not reinstated later by someone who reads only the record.
- **`NFR-001` cites two sections, because the limits now live in two.** `7bd6152` moved the general one-per-second-per-resource limit out from under *Retrieving zones*, which governs one endpoint, to `Architecture.md § Data sources and constraints`, which governs the API. The record's citation still resolved to a real heading, so no link check could fire; it simply pointed at a section that no longer said the thing. **The part that actually broke is the plural** — the statement read "the rate limits under *Retrieving zones*" and only one limit was left there — which is `@docs-writer`'s observation and worth keeping, because a plural is a load-bearing word that no tooling reads. `Source` now carries both sections, the per-resource criterion re-points to *Data sources and constraints*, and the all-zones interval criterion stays under *Retrieving zones* where that limit still lives. The `Risk`'s back-reference — "the interval that section states" — was correct while `Source` held one entry and ambiguous the moment it held two, so it now names its section outright.

**Batch 3 signed off, 4 August 2026 — all 21 records `draft` → `to-build`.** `FR-019` … `FR-034` and `NFR-001` … `NFR-005`, on the Owner's sign-off brokered by `@engineering-lead`, with the two repairs above applied first: `FR-022`'s repair was a condition of its signature, and `NFR-001`'s became due rather than pending when `7bd6152` landed. **Straight to `to-build` and not through `approved`**, as batches 1 and 2 each were, because `@requirements-reconciler` is dormant and there is no classification step for a record to wait in — no record in this corpus has ever held `approved`. That sign-off left the corpus at 39 records, all `to-build` or later, with `draft` empty — the state it held until batch 3b filed on 6 August 2026.

**Batch 3b filed, 6 August 2026 — `FR-035` … `FR-037`, `draft`.** Four obligations that `ff03613` added to `Architecture.md` **thirty-seven seconds after batch 3 was filed**, all four citing sections batch 3 had already swept. They are **traced, not invented**: the same commit whose retraction forced `FR-022`'s repair also *added* obligations, and a batch that takes the retraction while leaving the additions has read half a diff. Three became records and the fourth is a considered rejection below. No register extension was needed — all three are `Turf data integration`, whose registered scope is already *the fields whose absence or round scope constrains what can be concluded*, and `FR-030` with `FR-031` are the standing precedent for absence-handling records living there. `Zone data fidelity` was weighed for `FR-037` and rejected: that category owns the limits the data's nature places on the cost model and the optimizer, and a region subkey reaches neither.

- **`FR-035` binds the sync's write path, which `FR-022` does not reach.** `FR-022` puts the refresh on a schedule and off the request path; a job that satisfies it perfectly and writes only ids it has not seen before leaves every held zone's `dateCreated` frozen at first sight. The obligation is stated as *the value stored* rather than as an upsert keyed on `id`, because the cited section offers that as one sufficient way to meet the need rather than as the need.
- **`FR-036` is one record and not two, and the pair it was measured against is `FR-030` with `FR-031`.** Those two split because a correctly carried absence can still be misread downstream, so each half is satisfiable while the other fails. Here there is no such independence witness: **rejecting the record is itself a form of treating the absence as a fault**, so the implication runs one way only, which is a specialization and not a branch. The construction that *is* independent — an ingest substituting a value, which escalates nothing yet destroys the absence — **is not in this source** and is owed to the batch scoped to `SPECIFICATION.md § Zone attributes and user-defined value`, in `FR-030`'s shape. Flagged rather than authored, because authoring it here would admit scope through a side door.
- **`FR-037` is `COULD`, and carries a safety guard it was not authored with.** No consumer of a zone's country exists in the first release: `Architecture.md § D5` scopes an OSM *extract* by country, which is a bounding data decision rather than a per-zone attribution, and `SPECIFICATION.md § Geographic scope` argues against a country-keyed gate rather than for one. `COULD` on `FR-025`'s precedent — the path is not excluded, so the obligation binds the moment anything requires a country, and `WON'T-now` would file no story and leave a later aggregate free to be silently short. `Risk` is omitted and its argument folded into `Rationale`, as `FR-025` does. **The guard is the part worth recording.** `SPECIFICATION.md § An unknown speed limit fails the check` forbids resolving a jurisdictional speed-limit tag against national defaults, in these words: the resolved figure *"would rest on a country attribution supplied by the resolver rather than by the data"*. `FR-037` obliges the system to manufacture exactly such an attribution for every zone. It is not a contradiction — the record licenses no use — but read as *the corpus guarantees a country per zone* it is a standing invitation to build the lookup that section forbids absolutely, and the failure would arrive as a plausible limit carrying no sign it had been inferred. The record now bounds its own output to aggregates and names that section.

**The fourth obligation is a considered rejection, and no ID was issued for it.** `Architecture.md § API version` records that `GET /v5` returns **406** to a JSON `Accept` header — content negotiation, a different axis from `FR-019`, which binds which version every request names. It is rejected on **Necessary**. `Architecture.md § Data sources and constraints` and its subsections name every endpoint this system uses and the index path is not among them, so an obligation about how to read a response to a request the system never issues is **vacuous** — and a criterion whose antecedent never fires is the one kind that can never fail, which is the defect `NFR-001`'s amendment was rewritten for. Three constructions were tried and each rejected: *how availability is concluded* invents failure handling that `Architecture.md § Still owed by this document` has not written; *where the JSON `Accept` header is applied* is an `Appropriate` reject, naming an HTTP-client choice to protect an outcome nothing threatens; *what a 406 may not be read as* is the honest residue and is still vacuous. The section's own words give it away — *"worth knowing before that 406 is read as an outage or an auth fault"* addresses a human debugging, not a running system. **The residue lands on the failure-handling debt already recorded below**, and becomes a real obligation the moment failure handling is written such that the system probes the API. **`FR-038` is therefore unissued and `Next free` skips nothing** — allocation is highest-ever-issued plus one, and `FR-037` is the highest.

**Fifth considered zero — the NFR lane, on all four `ff03613` passages, 6 August 2026.** Scoped to those passages and no wider. The strongest candidate was `Estimate accuracy` on the `dateCreated` passage, and it fails on grounds any one of which is sufficient: the obligation is a write path rather than a target; the only honest bar is exact agreement between the stored value and the value the API publishes, which is the functional record restated as *count of X: zero* — the shape batch 3's third de-confliction bullet already ruled functional; the threefold inflation is a measurement, barred from a criterion by override 1; and that category's registered scope is the calibration of **presented** estimates, while the takeover rate is never presented and feeds a multiplier `CalculationSpecification.md § Proposed adjustment` states is a proposed default, so an accuracy bar anchored to it would harden a proposal in breach of override 2. `Coverage and data quality` was declined for the `type` and `region.country` passages on the third zero's own reasoning — that category's trigger is **thin** map or elevation data with confidence falling as it thins, and neither passage is thin data nor moves any confidence. **What would overturn this zero is named:** an accuracy or freshness tolerance for a derived rate, authored into `CalculationSpecification.md`. None exists, and none was invented.

**`Portability` on the country passage was weighed seriously and deferred, not dismissed.** The zones lacking `region.country` are not scattered — Ireland, Spain, Australia, Italy and France head the list — so a country-keyed aggregate does not lose a rounding error, it loses France, which is `SPECIFICATION.md § Geographic scope`'s *"must not be a system that only works in certain countries"* in miniature. It is deferred on the second zero's reasoning: the obligation is created by that section, not by the Architecture paragraph observing a consequence of it, and `SPECIFICATION.md § Non-functional requirements` is already recorded below as the corpus's largest unworked NFR source. Authoring now would let that batch find `Portability` claimed by a record scoped to one Turf subkey. **Recorded as a debt with its home named**, so the owed batch finds this rather than re-deriving it.

**Batch 3b returned zero `human-judgement` records, and the zero is argued rather than accumulated.** The corpus stands at 1 judged record in 42. The look was run per record before any method was chosen, as the skill requires, and what was searched for is recorded so the next reviewer need not repeat it: a maximum permitted age or divergence for a stored `dateCreated`, and any tolerance on the takeover-rate denominator (`CalculationSpecification.md § Takeover rate` states the formula and says outright that its own zero-divisor guard has no value set; the twelve-month figure under `CalculationSpecification.md § Proposed adjustment` bounds a *zone's age*, not a stored value's freshness, and is proposed besides); a data-completeness threshold, a permitted missing-field rate or an alerting threshold for `FR-036`; and any coverage bar on an aggregate for `FR-037`, where the only aggregate in that document is the **spatial** neighbourhood under `CalculationSpecification.md § The activity baseline`, which cannot be written against a country grouping. **Nothing was found, and nothing needed to be.** This batch is the data plane, and the data plane offers **discipline rather than quality**: update a field, do not escalate an ordinary absence, do not lose a zone from a group. Each has a crisp pass/fail with no standard of judgement in it, and `human-judgement` is the method for a residue after a quality attribute is established — here no quality attribute survived, so the method was never in play. **A judged record forced into this batch would have been a fabricated standard, which is the failure the method exists to prevent, running backwards.**

**Owed by batch 3b, and deliberately not authored.** Three debts, each with a stated home. **Whether the sync owes a refresh on fields beyond `dateCreated`** — `Architecture.md § Retrieving zones` observes the same zone's coordinates and `totalTakeovers` moving and warns against a write path that skips fields it assumes constant, but states the obligation for one field only; `FR-035` is bound to that field, and the general form is a ruling rather than an inference. **The `FR-030`-shaped companion to `FR-036`**, owed to `SPECIFICATION.md § Zone attributes and user-defined value`. **The `Portability` record on country coverage**, owed to `SPECIFICATION.md § Non-functional requirements`, per the deferral above.

**What no zero covered, recorded so the emptiness above is not over-read.** `SPECIFICATION.md § Genuinely out of reach or out of scope` is cited by `FR-005` and is `FR-012`'s sole source, but it entered batch 1 only as a corroborating second citation and was never swept as a section: five of its six bullets carry no requirement at all, and `FR-012` arrived from an Owner ruling rather than from a batch scoped to it. No NFR question has been put to it, and the absence of one there is **unexamined rather than a considered zero of its own**. That section is owed a batch of its own — an earlier wording of this paragraph described batch 1 as covering "the two source sections behind `FR-001` … `FR-006`", which counted a citation as a sweep and is corrected here.

Owed for the same reason, and noticed while `FR-012` was authored: *The planning player* states that the tool is used in a planning session rather than while driving, "which relaxes how quickly the first answer must appear". A relaxed target is still a target — but the obligation's home is `Architecture.md § Response time and progressive results`, not the section observing it, which is the same reasoning that produced the second zero below. **A `Response time` NFR is owed when a batch scoped to that Architecture section runs**, and is deliberately not allocated here.

**`SPECIFICATION.md § User time constraints` is owed a batch of its own, and this debt is not merely an unswept section.** Batch 2 actively deferred obligations to it: `FR-013`, `FR-017` and `FR-018` each disclaim in their `Rationale` an obligation that section owns — that there must always be at least one journey alternative within the user's stated additional-time limit — naming it as the single definition of the allowance rather than restating it. The consequence has to be written down plainly, because it cannot be seen from the records themselves: **until that batch runs, no record in the corpus obliges the offered set to be non-empty**, and every batch-2 record is vacuously satisfiable by a system that offers nothing at all. Owed to the same batch and deferred for the same reason: the allowance itself, the absolute ceiling over it, the value-based justification a stretch alternative requires, and a stretch alternative's identification as one.

**A second hole of the same shape, and it is not the one above.** Found by `@requirements-story-organizer` while cutting batch 2's stories, and recorded here because it cannot be seen from any record on its own. The debt above is that the offered set may be **empty**; this one is that it may be **singular**. Every batch-2 criterion is conditioned on alternatives *having been produced* — `FR-013`'s first criterion opens *given a journey for which more than one alternative … has been produced* — and **nothing anywhere in the corpus obliges the system to produce more than one alternative in the first place**. A search that only ever yields one satisfies `FR-013` without ever putting a choice in front of a user, which is the whole of what that record exists to secure.

The obligation's home is `Route alternative generation`, which is `reserved` on the register above: no file, no records, and the section behind it unswept. **It is owed to the batch scoped there**, and it is deliberately not authored now — writing it here would be admitting scope through the side door that story creation opened, and the register entry exists precisely so it is not. Recorded rather than storied, and carried on `#12` as an openly-stated hole so no worker reads that story as guaranteeing a plural result.

**Owed by batch 3, and deliberately not authored.** Four debts, each with a stated home, so none of them is later mistaken for an oversight.

**The honesty residue of confidence against data age.** A `human-judgement` record was authored — that a recommendation built from ageing Turf data must not present a confidence overstating how current it is — and is **held unfiled pending an Owner ruling**, not rejected. `SPECIFICATION.md § Confidence and uncertainty` enumerates ten uncertainty drivers and every one is a source of error in the *travel-time estimate*; data age is not among them, and stale ownership does not make a time estimate wrong, it makes a *value* estimate wrong. So the open question is not whether that enumeration is incomplete but whether it is the right standard at all. `NFR-002` carries the machine-checkable relation; the residue is owed to the batch scoped to `SPECIFICATION.md § Confidence and uncertainty`.

**Owed to the `Call budget` batch** — `Architecture.md § The call budget`, batch 4: the per-journey Turf call bound; the one-call properties of held-zone and region-lord retrieval; and the over-tiling concern in *Retrieving zones* — splitting a corridor into tiles far below the permitted area — which is a call-volume claim with no threshold that was not invented.

**Owed to failure handling, which `Architecture.md § Still owed by this document` has not written:** what the local copy is, and what may be concluded from it, when a sync fails or completes partially. **The debt stands; its original ground does not.** This entry was written against a partial download that timed out, recorded in `Architecture.md § Retrieving zones` as an observed fact — and that section has since retracted the observation as no longer reproducing. What the retraction removes is the *instance*, not the *hole*: a sync can still fail or complete partially for reasons the timeout was only one of, and neither that section nor any other states an obligation about the result. The ground is now the silence rather than the incident, which is the stronger of the two and should have been the ground from the start.

**`SPECIFICATION.md § Non-functional requirements` is an entire unswept section**, holding *Platform and mobile-first design*, *Geographic scope*, *Languages*, *Estimate accuracy and calibration* and *Time of day and traffic*. It is the corpus's largest unworked NFR source and it is owed a batch of its own.

**Why the second zero, recorded so it is not re-litigated.** That section names the one-home-per-model invariant while complying with it — the scoring model is defined once, in `CalculationSpecification.md`, and not restated. The obligation is created by `docs/README.md § Conventions`, not by the section observing it, so an NFR sourced from the observation would cite the wrong home; and narrowing it to the scoring model alone would shard a corpus-wide invariant per occurrence, giving the one-home rule more than one home — the exact defect it exists to prevent. **One corpus-wide `Maintainability` NFR, verified by `inspection`, is owed when a batch scoped to `docs/README.md § Conventions` runs.** It is deliberately not allocated here.

**Third considered zero — `SPECIFICATION.md § Recommended journey alternatives`, 2 August 2026.** The section states one obligation of its own — curate the offered set, avoiding effective duplicates and alternatives with no meaningful trade-off — and everything quality-*sounding* in it is the standard by which that one duty is judged, not a second duty. "Meaningfully different", "effectively duplicates" and "meaningful trade-off" are the adverbs of the curation rule; the corpus's home for a standard of judgement is the `Verification` field of the record carrying the duty, never a companion record. An NFR restating them would be the same sentence filed twice, split only by verification method, and would collide head-on with the FR sweep of this same section. One obligation, one home.

No threshold exists to hang a metric on. "Several" is not a count, and nothing in `CalculationSpecification.md`, `Architecture.md` or `DESIGN.md` sets a number of alternatives to present or a distinctness bar between them. The nearest thing in the corpus — "a route alternative is not merely a variation caused by visiting one additional zone", `SPECIFICATION.md § General route alternatives` — is a *definition* rather than a threshold, and it governs the general-road-route layer, not the Turf-strategy layer this section governs. Any number filed here would have been invented.

The fallback is not `Coverage and data quality`, and this is the trap the section sets. That category degrades on **thin map or elevation data**, with confidence falling as it thins. This section degrades on **zone availability** — there genuinely is no attributed zone inside the user's limit — which is a fact about the world, not a deficiency in our data; the system knows it perfectly well and its confidence is unaffected. Filing it under that category would give "graceful degradation" two triggers and leave the later data-thinness batch working around a record that had already claimed the name. What the fallback obliges is an action in a branch, with an unhappy path and no quality target attached. That is functional, and it is `FR-018`.

The allowance, the absolute ceiling over it and the at-least-one-compliant guarantee are all sourced from `SPECIFICATION.md § User time constraints`, which states it is their single definition; they are owed to a batch scoped there. No `Response time` question was put to this section — that NFR is already owed to `Architecture.md § Response time and progressive results` and is not pre-empted here. An `Observability` record on how often the fallback fires was considered and rejected: no document asks for it, and authoring it would be inventing scope rather than reading it.

## How this folder works

- **IDs are immutable.** Never reused, never renumbered, never recycled after deletion. Whoever issues the next one takes it from the ledger above and nowhere else.
- **`INDEX.md` carries a row for every ID ever issued**, including retired ones. A retired requirement keeps its record, in place and in ID order, reading `Status: retired`; it is never deleted and never moved. There are no tombstones today because no ID has ever been retired — `FR-001` … `FR-034` and `NFR-001` … `NFR-005` are each contiguous with no gaps.
- **A retired record's `Resolved-by` freezes at its last value.** Retirement removes the requirement from the *Requirement → story* table in `TRACEABILITY.md`, so the link's single home disappears with it — but the field on the record and the column in `INDEX.md` keep whatever they last read rather than reverting to `—`. They are then the only surviving record of what was built for that requirement, and blanking them would erase it. A frozen value is not stale and is not a finding.
- **Records sit in ID ascending order inside their file, and that order never changes** — not on retirement, not on re-prioritization. A file's order is therefore a fact about IDs alone, and a diff never moves a record it did not touch.
- **The record is authoritative; every index is derived.** Where a row in `INDEX.md` and the record in a category file disagree, the record is right and the row is stale — a librarian finding, fixed in the index, never fixed by editing the record to match.
- **Records are filed verbatim inside fenced blocks**, preserving the field alignment of the canonical record in `requirements-authoring`. The librarian moves, groups, indexes and cross-links records; it never re-words one. Content questions go to `@requirements-engineer`.
- **Category files are named as the kebab-cased `Category` field**, verbatim and unnumbered, so a record names its file and a file names its category with nothing to look up, and adding a category never renames one.
- **A `Category` never spans both kinds, and the `Kind` column holds one value.** Raised as a finding on 3 August 2026 by `@requirements-librarian` and ruled the same day. `Turf data integration` had been registered `FR + NFR`, producing a file that held ten FRs and then `NFR-001` … `NFR-002`, and `requirements-authoring § Corpus layout` cannot be satisfied whole by such a category: it states in one breath that a file is named as its `Category` verbatim and that **FRs and NFRs share the folder, never a file**. The batch filed under the naming rule — the one traceability rests on, since a record names its file with nothing to look up — and marked the file explicitly non-precedent, which is why the ruling cost a re-file rather than a migration. `@requirements-engineer` proposed amending the skill so that one category may hold both kinds; **the Owner kept the skill as written and split the category**, on the ground that a file's kind then stays trivially checkable and the librarian's conformance sweep stays mechanical. `NFR-001` and `NFR-002` moved to `Outbound rate compliance` and `Data currency and confidence`, registered above with their reasoning.
