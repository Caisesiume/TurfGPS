# TurfGPS requirements

The front door to the requirements corpus. Every functional and non-functional requirement lives in exactly one category file in this folder, in the canonical record form defined by `requirements-authoring`. This page holds the three things that change rarely and that everything else in the folder depends on: the **category register**, the **ID allocation ledger**, and the folder's **invariants**.

Why a folder rather than a file: `docs/README.md § Requirements/`.

## Corpus state

**Corpus state — 7 August 2026, after batch 5.** 100 requirements, 20 categories in use. **62 are `to-build` and 38 are `draft`** — `FR-058` … `FR-093`, `NFR-006` and `NFR-007`, filed the same day and awaiting the Owner's signature, with two conditions attached to it that are recorded in the ledger below. This is the largest `draft` population the corpus has ever held and the first time `draft` has outnumbered a single earlier batch; nothing about it is a stall, and the batch's own ledger entry states what it is waiting on.

Earlier the same day, and before batch 5 was authored: `FR-055` … `FR-057` were **signed off by the Owner** and moved `draft` → `to-build`, and `@requirements-story-organizer` cut `#58` … `#60` for them under a Milestone that already existed.

Batch 4 — `FR-038` … `FR-054`, filed on 6 August 2026 from `SPECIFICATION.md § User time constraints` — was **signed off by the Owner the same day** and all seventeen moved `draft` → `to-build`. It is the largest batch the corpus has taken in one pass and the first to open five category files at once, two of them under names added to the register in the same pass. **Its story pass ran later the same day** — four epics and seventeen stories, `#41` … `#57`. Story creation follows sign-off as a pass of its own, and the interval between the two was the first this corpus has held with signed-off requirements and no story behind them; it lasted one pass and is closed.

**Two firsts came with the three records signed on 7 August 2026, and each is recorded where it bites.** `FR-055` is **the first record in this corpus amended after it was signed** — the argument for why that is not a re-opened obligation is in the ledger below, beside the other amendment records. And their sign-off opened **no committed interval at all** before their story pass — the two closed inside a single pass, so `Resolved-by: —` never meant *signed off, story pass due* for any of them, and batch 4 remains the only batch of which the corpus has ever held that state. That one is recorded in `TRACEABILITY.md`, beside batch 4's interval.

Batch 3b — `FR-035` … `FR-037` — was filed earlier on 6 August 2026 and **signed off by the Owner the same day**. All three are `Turf data integration`; no category was added for them and none was needed.

**Batch 3's sign-off, 4 August 2026.** The twenty-one records `FR-019` … `FR-034` and `NFR-001` … `NFR-005` were **signed off by the Owner on 4 August 2026**; nineteen were filed on 3 August 2026 and `FR-033` … `FR-034` on 4 August, added to the same batch on a ruling that closed one of its own open questions. They moved from `draft` **straight to `to-build`**, as both earlier batches did, because `@requirements-reconciler` is dormant and there is no classification step for a record to wait in `approved` for — no record in this corpus has ever held `approved`.

**Batch 3 was the first batch to sit in `draft` after a filing pass**, which was an ordinary state rather than a stall — it had simply never been observed here, because batches 1 and 2 were each signed off the day they were filed. `FR-001` … `FR-012` were signed off by the Owner on 1 August 2026 and `FR-013` … `FR-018` on 2 August 2026. Story creation has run for every **signed-off** requirement in the corpus: `@requirements-story-organizer` filed four epics and seventeen stories on 2 August 2026, in two passes, one per sign-off, five further epics and nineteen stories on 4 August 2026 for batch 3, three stories under existing epics on 6 August 2026 for batch 3b, four epics and seventeen stories later the same day for batch 4, and three further stories under an existing epic on 7 August 2026 for `FR-055` … `FR-057`. The links live in `TRACEABILITY.md`; each record's `Resolved-by` and the column in `INDEX.md` are views of them.

**Batch 3b sat in `draft` after its filing pass, and story creation was deliberately not run until the Owner signed.** Sign-off gates story creation, not the reverse, so `FR-035` … `FR-037` carried no epic and no story across the interval between the two passes. The signature came the same day, and `@requirements-story-organizer` then cut `#38` … `#40` under epics that already existed rather than under a new Milestone. This was the same shape batch 3 held between 3 and 4 August, and it is recorded again only because the corpus has spent most of its life with filing and story creation in step.

**All 62 signed-off records name at least one story in `Resolved-by`** — thirteen epics and fifty-nine stories carry them — and that was the whole corpus until batch 5 filed. **Batch 5's thirty-eight all read `—`, and it means what it has always meant: *not yet allocated*.** Sign-off gates story creation and not the reverse, so an unsigned record carrying no story is the correct state rather than an outstanding pass. The em-dash keeps its one meaning wherever it next appears: *not yet allocated*, never *unknown*. **The corpus holds no orphan, and it is worth being exact about why**: the orphan test applies only to a requirement whose epic is in flight, and there is no requirement without a story for it to apply to — the rule is unexercised today rather than satisfied by argument, and it will need running again the moment a filing pass puts a `—` back. **Sign-off was not the orphan test and did not become one** — what makes a `—` a finding is an epic in flight above it, so batch 4's seventeen were exactly as far from being orphans across the interval as they were before the signature, and `FR-055` … `FR-057` never held an interval in which the question could be put at all.

## The files in this folder

| File | Holds |
|---|---|
| `README.md` | This page: front door, category register, ID allocation ledger, folder invariants |
| `INDEX.md` | One index table per category, carrying a row for every ID ever issued |
| `journey-definition.md` | The `Journey definition` records — one file per category, named from the register below |
| `journey-initialization.md` | The `Journey initialization` records |
| `route-alternative-generation.md` | The `Route alternative generation` records |
| `turf-data-integration.md` | The `Turf data integration` records |
| `zone-data-fidelity.md` | The `Zone data fidelity` records |
| `candidate-identification.md` | The `Candidate identification` records |
| `access-classification.md` | The `Access classification` records |
| `route-construction.md` | The `Route construction` records |
| `cost-and-time-composition.md` | The `Cost and time composition` records |
| `multi-leg-budget-allocation.md` | The `Multi-leg budget allocation` records |
| `value-model.md` | The `Value model` records |
| `objective-selection-and-ranking.md` | The `Objective selection and ranking` records |
| `recommendation-set-composition.md` | The `Recommendation set composition` records |
| `recommendation-disclosure.md` | The `Recommendation disclosure` records |
| `safety-exclusions.md` | The `Safety exclusions` records |
| `coverage-and-data-quality.md` | The `Coverage and data quality` records |
| `classification-correctness.md` | The `Classification correctness` records |
| `data-currency-and-confidence.md` | The `Data currency and confidence` records |
| `outbound-rate-compliance.md` | The `Outbound rate compliance` records |
| `runtime-and-deployment-shape.md` | The `Runtime and deployment shape` records |
| `TRACEABILITY.md` | The requirement ↔ epic ↔ story matrix, both directions |

**Why the index is a separate file.** The allocation ledger is the only guard against ID reuse, and it must not sit below fifteen screens of index table where an author stops scrolling. The register and the ledger change rarely; the index changes on every filing. Keeping them apart means a routine filing does not rewrite the one file the whole RE family reads first, and two agents filing different categories do not serialize against a single file.

## Category register

The corpus's controlled `Category` vocabulary, and its only home. A `Category` value is legal if and only if it appears in the **Category** column below, copied verbatim. `@requirements-engineer` alone adds a name here — a new category is a decision, never a side effect of authoring. `@requirements-librarian` files each record under its `Category` and raises a record carrying a name not on this list as a finding, rather than filing it under a near-match.

**A name on this list is permission to file, not a promise that requirements exist.** Most rows read `reserved`: the source sections behind them have not been broken down yet. `reserved` means no file and no records, and nothing may be concluded from the absence — a category's absence from the *filed* set means unfiled, never illegal.

These names were consolidated on 1 August 2026 from coverage prompts that previously stood in four agent definitions, which named the same areas in four different phrasings and would have produced several files for one subsystem. Those lists remain in place as coverage prompts — questions an author asks per source section — and are explicitly no longer a source of category names.

**Extensions since.** `Value model`, added 1 August 2026 by `@requirements-engineer` while integrating `SPECIFICATION.md § The journey as an optimization problem`. The register as consolidated carried `Cost and time composition` with no counterpart on the value side, while the objective the whole product optimizes is value set against cost. Without it, everything the value half of that objective is built from — zone attributes, attribute rarity and preference, the rank-to-weight curve — would have filed under `Objective selection and ranking`, whose scope is how value and cost are *combined*, not what either is made of. Recorded here because a register extension is a decision, and a decision with no stated reason is indistinguishable later from a name someone coined in passing.

`Recommendation set composition`, added 2 August 2026 by `@requirements-engineer` while integrating `SPECIFICATION.md § Recommended journey alternatives`. The register carried `Route alternative generation` for producing corridors and `Objective selection and ranking` for the objective and the ordering under it, and nothing for the question that section actually answers: which of the alternatives produced are put in front of the user. Filing those records under `Objective selection and ranking` would have put set membership and set ordering in one file under a scope line that names only the second, and coverage per subsystem would have stopped being checkable for either. Recorded here because a register extension is a decision, and a decision with no stated reason is indistinguishable later from a name someone coined in passing.

`Turf data integration`, added 3 August 2026 by `@requirements-engineer` while integrating batch 3. The register as consolidated was seeded from the decision core and carried no category for the data plane at all — the layer between the Turf API and everything downstream of it. Batch 3 is the first batch to enter that layer. Filing these records under the decision-core categories would have put the integration contract in the same files as the optimizer's behaviour, and coverage per subsystem would have stopped being checkable for both. The boundary against `Call budget` is written into the scope line rather than left to be rediscovered, because the two abut and the line is not obvious.

`Zone data fidelity`, added 3 August 2026 by `@requirements-engineer` while integrating batch 3. Separated from `Turf data integration` deliberately. Its records bind the optimizer and the cost model rather than the integration layer, and folding them in would mean a reviewer asking whether the cost model assumes a minimum inter-zone distance has to read the integration file. It is small — three records — and the register already carries a category of one; size is not the test, subsystem is.

`Outbound rate compliance` and `Data currency and confidence`, added 3 August 2026 by `@requirements-engineer` on the Owner's ruling of the same day: `Turf data integration` splits by record kind, rather than the skill's one-kind-per-file rule bending to admit a category holding both. They are the NFR half of what batch 3 filed as a single `FR + NFR` category, and they close the open finding recorded under *How this folder works* below.

**The split is by subsystem too, not by kind alone, and that is why honest names exist for it.** A doubled register — one name per subsystem on the FR side and the same name with a word appended on the NFR side — would have made `Category` a restatement of `Kind` and would have said nothing about the system. It was avoidable because the two records were never held together by their subsystem in the first place: they were held together by their *source section*, and the FR file had already drawn the line between them in prose. `FR-024`'s `Rationale` reads *what the system may conclude from a copy of that age is a confidence question, and it is `NFR-002`'s* — the boundary between what the pipeline tolerates and what it may claim, written by an author with no idea it would become a filing boundary. The register now states that line instead of leaving it in one record's rationale.

**The NFR half of this register was never a subsystem vocabulary**, which is the deeper reason no doubling occurred. `Response time`, `Estimate accuracy`, `Call budget`, `Privacy`, `Portability`, `Observability` and `Maintainability` each name a **quality**, where every FR name names a **subsystem**. One column has carried two vocabularies since consolidation and `Kind` is what tells them apart — so `Turf data integration`, registered as `FR + NFR`, was the sole row spanning both, and the ruling's real effect is to remove the one exception rather than to introduce a division. Neither new name is a Turf-integration name with a qualifier: one names a compliance obligation at the egress edge and would hold a rate record for any limited upstream, the other names an age-to-confidence relation and would hold one for any ageing source.

`Runtime and deployment shape`, added 3 August 2026 by `@requirements-engineer` while integrating batch 3. Added to carry the NFRs that repository scaffolding satisfies, following the Owner's ruling of 3 August 2026 that `DELIVERY.md` carries **no traceability exception for scaffolding** and that scaffolding must be backed by real requirements. No existing category fitted: `Portability` is about adding a country as an adapter, `Maintainability` about the cost of the next safe change, and `Platform support` about the browsers the client must work in. What the build produces, and which deployment topologies the architecture rules out, had no home.

`Recommendation disclosure`, added 6 August 2026 by `@requirements-engineer` while integrating batch 4. Three of that batch's records oblige what the user must be **told** about an alternative — that one above the stated limit is identified as a stretch, that its explanation names what the limit was exceeded for, and that the additional time shown for a multi-leg journey is the total. None of them is a membership question, and `Recommendation set composition` disclaims presentation **in its own scope line**, which the Owner's ruling of 1 August 2026 on `FR-010` reinforces from the other side: the obligation there is on the offered set and never on presentation order. Filing them under it would have contradicted the register in the act of using it. The boundary against `DESIGN.md` is written into the scope line rather than left to be rediscovered, because it is the line this category will be pushed across: what must be conveyed is a requirement, what it looks like is not.

`Multi-leg budget allocation`, added 6 August 2026 by `@requirements-engineer` while integrating batch 4. `Cost and time composition` was proposed for it and rejected. That category composes a journey's cost from routing, walking and manoeuvre components; allocating an allowance across legs is the mirror operation, and folding it in would mean a reviewer asking whether stop cost is composed correctly has to read three records about dividing a budget. The alternative — bolting the allocation onto that category's scope line — would put two subsystems under one name, which is what the register exists to prevent. It is a category of three; `Zone data fidelity` already established that size is not the test.

**`Route alternative generation`'s scope line is widened, not extended, and this is a repair.** It read *producing the candidate corridors a journey may take*, while the second-hole debt recorded below had already assigned it a second obligation in plain words — *nothing anywhere in the corpus obliges the system to produce more than one alternative in the first place*, whose home that debt names as this category. The register and the ledger have therefore disagreed since 2 August 2026, and `FR-042` is what forced the reading: it obliges the search not to decline **by reason of the size of the stated limit**, and its testable floor is a produced alternative that captures a zone, which is not a corridor. Raised by `@requirements-fr`, which observed that a librarian reading the old scope line alone would have had grounds to flag the category's first inhabitant as misfiled. Widened to the ledger's reading rather than the ledger narrowed to the register's, because the debt is the older and better-argued of the two and nothing else in the corpus can hold it. The boundary against `Recommendation set composition` is now stated on both rows: this category owns the search's output, that one owns which of it reaches the user.

**Why `FR-042` is not `Recommendation set composition`, recorded because it is the plausible misfiling.** It reads as a do-not-withhold record and `FR-018` is one, filed there. The difference is load-bearing: `FR-043`, the at-least-one-compliant guarantee, is **conditioned on production** — *where the system has produced* — so a system that produces nothing at a short budget satisfies it vacuously. `FR-042` is what closes that, and it can only do so from the production side. Filed as an offering record it would have left the batch with the same hole it was commissioned to fix, one level down.

`Route construction`, added 7 August 2026 by `@requirements-engineer` while integrating batch 5. The register carried `Route alternative generation` for the search's output and `Objective selection and ranking` for the objective and the ordering under it, and nothing for the subsystem `SPECIFICATION.md § Route construction and optimization behaviour` actually specifies: how **one** alternative is assembled from a general route and a set of classified candidates — which zones are combined, the order they are visited in, how the marginal cost of adding a zone to an existing sequence is obtained, and the rule that a detour's cost is routed and never inferred from geometry. Three existing names were weighed and each fails on the same line. `Route alternative generation` owns whether alternatives are produced **at all and in what number**, which says nothing about how one is built, and widening it again would put the search's cardinality and the assembly of a single member under one name one pass after that scope line was repaired for the same kind of overload. `Cost and time composition` composes a cost from components already identified; the marginal cost of an *addition to a sequence* presupposes the sequence, and the sequence is what this category decides. `Objective selection and ranking` owns which assembled alternative wins, not the assembling. Recorded here because a register extension is a decision, and a decision with no stated reason is indistinguishable later from a name someone coined in passing.

**The boundary that will be pushed is against `Cost and time composition`, and it is stated on both rows.** A record obliging that a detour be routed rather than inferred reads as a costing rule and is not one: it binds **where the number comes from** when a zone is added to a sequence, which is a construction act, while composing that number out of driving, walking, takeover and manoeuvre parts is the other category's. The test is the one the register already uses elsewhere — a reviewer asking *how is a stop's time made up* must not have to read three records about visit order to find out.

| Category | Kind | File | Scope | State |
|---|---|---|---|---|
| Journey definition | FR | `journey-definition.md` | The destinations a request must and may name, the order they are visited in, their preservation through optimization, and the mode of travel between them | **in use** |
| Journey initialization | FR | `journey-initialization.md` | How a journey is entered and a planning session begins: input capture, the time budget, validation, session start. **What a journey must and may name is `Journey definition`, not this** — the split is the act of entering against the thing entered | **in use** |
| Route alternative generation | FR | `route-alternative-generation.md` | Producing the alternatives a search yields: the candidate corridors a journey may take, and whether alternatives are produced at all and in what number. Distinct from `Recommendation set composition`, which decides which of them reach the user — this category owns the search's output, that one owns the offered set | **in use** |
| Turf data integration | FR | `turf-data-integration.md` | How zone, player and region data is obtained from the Turf API and what may be relied on from it: which source answers which question, the local synced copy and the staleness the pipeline tolerates in it, the shape constraints the endpoints place on a single request, and the fields whose absence or round scope constrains what can be concluded. Two neighbours abut it and neither is folded in: **rate over time against the API is `Outbound rate compliance`; per-journey call volume is `Call budget`** — this category owns what is fetched and what it means, never how often it may be asked for | **in use** |
| Zone data fidelity | FR | `zone-data-fidelity.md` | What the system may and may not assume about an individual zone, and about the relationship between two zones, given what the Turf API exposes and what it merely *guides*. Distinct from `Turf data integration`, which owns how that data is obtained and kept current — this category owns the limits the data's nature places on everything downstream of it, including the cost model and the optimizer | **in use** |
| Candidate identification | FR | `candidate-identification.md` | Selecting which zones enter evaluation, and bounding that set | **in use** |
| Access classification | FR | `access-classification.md` | Deciding whether and how a zone can be reached and stopped at | **in use** |
| Route construction | FR | `route-construction.md` | Assembling one journey alternative from a general route and a set of classified candidate zones: which zones are combined, the order they are visited in, how the marginal cost of adding a zone to an existing sequence is obtained, and the rule that a detour's cost is routed rather than inferred from geometry. Distinct from `Route alternative generation`, which owns whether alternatives are produced at all and in what number — that category owns the set the search yields, this one owns how one member of it is built — and from `Cost and time composition`, which composes a stop's cost out of its parts once the sequence exists | **in use** |
| Cost and time composition | FR | `cost-and-time-composition.md` | Composing stop cost and journey cost from routing, walking and manoeuvre components | **in use** |
| Multi-leg budget allocation | FR | `multi-leg-budget-allocation.md` | Dividing a journey's additional-time budget across its legs before any leg is optimized, and redistributing what a leg does not use. The mirror of `Cost and time composition`: that category composes a cost from its components, this one decomposes an allowance into shares. Distinct from `Safety exclusions`, which owns the journey-level target and ceiling the shares must sum within, and from `Journey initialization`, which owns the capture of the budget being divided | **in use** |
| Value model | FR | `value-model.md` | What a journey's Turf value is made of: the zones counted toward it, the attributes that weight them, and the user's stated preferences over those attributes. The mirror of `Cost and time composition`; the two are combined by `Objective selection and ranking`, which owns neither | **in use** |
| Objective selection and ranking | FR | `objective-selection-and-ranking.md` | The objective a plan optimizes, and the ordering of alternatives under it | **in use** |
| Recommendation set composition | FR | `recommendation-set-composition.md` | Which journey alternatives reach the user and what the offered set must hold: how many, how varied, what is dropped as redundant or dominated, and what may never be withheld. Distinct from `Route alternative generation`, which owns the search's output — whether alternatives are produced at all, in what number, and the corridors they run on — and from `Objective selection and ranking`, which decides the objective and the order. This category decides membership of the offered set, not its ranking and not its presentation, which is `Recommendation disclosure`'s | **in use** |
| Recommendation disclosure | FR | `recommendation-disclosure.md` | What the system must tell the user about a recommendation it offers: the figures it must show, the labels an alternative must carry, and the reasons it must give for what the optimizer did. Distinct from `Recommendation set composition`, which decides membership of the offered set and disclaims presentation in its own scope line — this category owns what is said about an alternative, never how it looks, which is `DESIGN.md`'s | **in use** |
| Review and replacement | FR | — | The user's review loop: rejecting a zone and receiving a replacement | reserved |
| Hand-off and dispatch | FR | — | Delivering a chosen plan to the device and the navigation app that will drive it | reserved |
| Persistence and staleness | FR | — | Storing a plan, retrieving it, and what makes a stored plan no longer trustworthy | reserved |
| Safety exclusions | FR | `safety-exclusions.md` | The enforceable exclusions and the absolute ceiling, as behaviour the system must exhibit | **in use** |
| Response time | NFR | — | How long the initial solve and the review-time replacement may take | reserved |
| Estimate accuracy | NFR | — | Calibration of presented estimates, and the width of the ranges carrying them | reserved |
| Coverage and data quality | NFR | `coverage-and-data-quality.md` | Graceful degradation where map or elevation data is thin, and confidence falling with it | **in use** |
| Data currency and confidence | NFR | `data-currency-and-confidence.md` | How the age of the data a recommendation was built from bounds the confidence the system may record for it. Its trigger is the **age** of data that is otherwise complete, which is what separates it from `Coverage and data quality`, whose trigger is data that is **thin**; and its subject is the confidence carried by the recommendation rather than the calibration of the time estimate inside it, which is what separates it from `Estimate accuracy` | **in use** |
| Classification correctness | NFR | `classification-correctness.md` | The product's stated measure of success: no zone classified confidently and wrongly | **in use** |
| Call budget | NFR | — | Bounded and known per-journey external call volume | reserved |
| Outbound rate compliance | NFR | `outbound-rate-compliance.md` | Holding the system's outbound request rate within the limits an external service publishes, counted across every caller inside the system at once — solve sessions and background jobs together, since the limit is the other party's and applies to the system as a whole. Distinct from `Call budget`, which bounds the per-journey call **volume** the system chooses for itself: this category answers to a ceiling set elsewhere, measured per unit time, and owns the aggregation point at which it is honoured | **in use** |
| Privacy | NFR | — | What a stored plan holds, and what the system declines to hold at all | reserved |
| Portability | NFR | — | Adding a country as an adapter rather than a rewrite | reserved |
| Platform support | NFR | — | Mobile-first web: desktop and mobile, iOS and Android browsers | reserved |
| Runtime and deployment shape | NFR | `runtime-and-deployment-shape.md` | The form the system is built and run in: what the build produces, how the client is served, the statefulness the solve session imposes, and the deployment topologies the architecture rules out. Distinct from `Platform support`, which is about the browsers the client must work in | **in use** |
| Observability | NFR | — | What must be measurable about a running system, including the decisions the design earmarks for measurement | reserved |
| Maintainability | NFR | — | The cost of the next safe change, including the one-home-per-model invariant | reserved |

A category file is created the first time a record is filed under its name, and the **File** column changes from `—` to the filename in the same pass.

## ID allocation ledger

**Next free: FR-094 · NFR-008**

**Allocation is highest-ever-issued plus one, never lowest-unused**, so a retirement cannot free a number even if a tombstone is lost. One ascending sequence per prefix, the two counters running independently.

**`FR-000` and `NFR-000` are permanently reserved and are never allocated.** They carry the worked examples in `requirements-authoring`, which has to illustrate the record form without any illustration answering to a live requirement's identity. The reservation costs nothing: both sequences run upward from `001`, so neither number is reachable by the rule above. Until 3 August 2026 the skill's canonical example was numbered `FR-021`, which batch 3 then issued to a real record — `grep FR-021` returned one real requirement and one fictional one, and `FR-032`'s `Rationale` cites `FR-021` by number, so it resolved to either. **The example was renumbered and the record was not**, which is the only order the immutability rule permits: an allocated ID is immutable, and the example's number was never an allocation.

**The NFR sequence opened on 3 August 2026, with `NFR-001` … `NFR-005`.** Until batch 3 no NFR had ever been issued, and the emptiness was a recorded outcome rather than a gap: `@requirements-nfr` returned a considered zero for batch 1, scoped to `SPECIFICATION.md § Primary use case` and its subsection *The planning player* — neither states a quality attribute with an obligation attached — a second considered zero, argued and accepted, for `SPECIFICATION.md § The journey as an optimization problem`, the section behind `FR-007` … `FR-011`, and a third for `SPECIFICATION.md § Recommended journey alternatives`, the section behind `FR-013` … `FR-018`, argued at the foot of this ledger. **All three remain correct in their own scope and none is overturned by this batch.** They are still the reason `NFR-001` was free until now, and they are kept in full below for that reason.

The drought ended where those zeros did not reach. Batch 3's NFRs came from `Architecture.md § Data sources and constraints` and `Architecture.md § Technology decisions` — sections none of the three zeros covered, and neither of them a section a zero was ever put to. Nothing about the sequence opening here says a zero was wrong; it says a different document was swept.

**Batch 3's de-confliction, recorded so it is not re-litigated.** Four authored records did not file, and an ID was issued for none of them. The FR and NFR lanes swept the same Architecture sections in parallel, so the overlaps below are the ordinary product of that and not a fault in either lane.

- **A functional record on Turf rate limits was dropped in favour of `NFR-001`.** Both lanes authored the same obligation and both chose `test`. A bound on a rate is a quality, and `NFR-001` alone names the aggregation point — a limiter held per session or per worker satisfies a per-caller criterion and still breaches a limit the API applies to the system as a whole.
- **A non-functional record on solving against a refreshing or stale copy was dropped in favour of `FR-022` and `FR-024`.** It duplicated `FR-024` nearly word for word, and its one distinct clause — that a solve does not wait on the sync — is already `FR-022`'s second criterion.
- **A non-functional record forbidding an assumed minimum inter-zone distance was ruled functional** and re-authored as `FR-032`. A prohibition on a behaviour is not a quality attribute; an acceptance criterion of the form *count of X: zero* would make every prohibition an NFR. The content survived because `FR-021` genuinely does not reach logic that obtains no distance at all.
- **A fourth considered zero, on `Architecture.md § API version`, is accepted in its own lane** — the NFR lane returned it, and there is no quality attribute in that section. `FR-019` stands over it because it carries what neither Architecture section does: that **exactly one** API version is named, on error and fallback paths as well as primary ones.

**Amended after filing, 3 August 2026.** Recorded because a field changed after the batch was filed, and a diff alone would not say why. The first two follow the Owner's rulings of that day; the third is a conformance reject raised by the sweep those rulings forced.

- **`FR-025` drops from `SHOULD` to `COULD`.** `Architecture.md § Retrieving zones` makes the local synced copy the source for corridor resolution, while `Architecture.md § Volatile and optional fields` calls any locally cached copy a snapshot; the Owner ruled that the sync is the source and that no per-journey refresh is required for the first release, which puts the bounding-box endpoint — the only endpoint `FR-025` constrains — off the first-release critical path. The supporting measurement is `@data-architect`'s: **1.28% of zones change `totalTakeovers` per hour** and 23,119 of them, or 16.6%, per day, against a sync feeding data that is either near-static or rated over months. A per-journey refresh buys nothing measurable and adds a live API dependency inside every solve. **`COULD` and not `WON'T-now`**: the endpoint is not excluded — the same section keeps it as the fallback and as the means of refreshing volatile per-zone fields — so the obligation binds the moment that path is built and the statement stays `shall`. `WON'T-now` would file no story at all and leave a fallback free to construct requests the API rejects.
- **`NFR-001`'s second acceptance criterion now names the all-zones endpoint by role rather than by path.** It read `GET /v5/zones/all`, which made the record a second home for the API version — a constant whose home is `Architecture.md § API version`, and which `FR-019` exists to bind by citation, its `Rationale` saying exactly that. Raised by `@requirements-librarian` during the re-file sweep and rewritten by `@requirements-nfr`. **The reject is `Verifiable`, not merely `Conforming`**, which is why it was a rewrite and not a note: on a version bump the criterion would still have *passed*, vacuously, because a deployment on the next version issues no `/v5/` requests at all and the interval between zero requests satisfies any minimum. A criterion that reports green while measuring nothing is the one kind that can never fail. A `/v[0-9]/` sweep of the corpus returned that line and no other, and `FR-022` had already named the same endpoint version-free, so the record was the outlier against its own batch. Caught in `draft`: no story, no index row and no signed record moved with it.
- **Nothing is amended for `dateCreated`, and nothing needed to be.** Whether the live API still returns the field is being verified against the endpoint on the Owner's ruling of the same day; the January 2025 dump carries it, which establishes the dump rather than today. **No record in this corpus names `dateCreated`** — the field appears only in the upstream documents, under `CalculationSpecification.md § Takeover rate` and the sections citing it — so nothing filed rests on the answer and nothing was held back pending it. Recorded so that the batch which eventually reaches takeover rate finds the verification rather than re-deriving the question. **Both halves are closed by batch 3b, 6 August 2026, and the note did its job.** `ff03613` answers the verification — `dateCreated` is on 100.00% of records across the complete response, counted rather than sampled — and `FR-035` makes the second sentence false on filing, being the first record in this corpus to name the field. Left standing rather than deleted, because what it recorded is why the question was not re-derived.

**Added to batch 3 after filing, 4 August 2026.** `FR-033` and `FR-034`, on the Owner's ruling of 3 August 2026 closing the batch's open third question: plan against whatever copy of the zone set exists and mark the result as built from stale data, refusing only where no copy exists at all. This is authoring rather than amendment — the obligation belongs to `Architecture.md § Retrieving zones`, a section batch 3 swept, and the batch was still `draft` when they were written, so no signed record moved and no story was disturbed. **The ruling produced two records rather than the one commissioned**, and the split is recorded here because a one-record commission returning two reads as drift unless the reason is written down. Planning past the bound and recording what the plan was built from are two separately testable outcomes on the **same** branch, so a statement joining them would be half-satisfiable by the half that keeps the product working — which is the half nobody forgets to build. Planning and refusing, by contrast, are two branches of one gate reading one input, whether a copy is held, and they stay together in `FR-033` exactly as `FR-012`'s three criteria do. Both are `MUST`: the ruling permits degradation **on condition** that it is disclosed, and a split made for singularity must not quietly demote the condition below the behaviour it conditions.

**No register extension was needed for either, and one was considered.** `FR-034`'s subject is the result rather than the data plane, which argued for a category of its own; it was rejected. `Data currency and confidence` is registered `NFR` and cannot hold a functional record. A new functional category for one record would put the marking rule in a different file from the planning rule it conditions, so a reviewer asking what happens past the bound would read two files for one ruling, and it would pre-empt `Persistence and staleness`, which owns whether that fact survives storage and retrieval. The deciding line is one the register has already drawn: `FR-024`'s rationale puts what the system may **conclude** from a copy of that age on the confidence side, with `NFR-002`. `FR-034` concludes nothing — it records a fact about provenance — so it sits on the pipeline side of a line already drawn, and needs no new name.

**Repaired before sign-off, 4 August 2026 — two records, one failure mode.** Both are sources that moved under a filed record, which is not an authoring error and is exactly what requirements management exists to catch. Neither record was wrong when written; both were wrong when read, and nothing in either record could have told a reader so. Recorded together because the pair is the argument: a corpus is not made correct by careful authoring alone, and the interval in which a record can silently rot is as short as the second case shows.

- **`FR-022`'s `Risk` loses its first clause.** It rested the obligation on an all-zones download "slow enough to have timed out on a partial transfer" — an observation `Architecture.md § Retrieving zones` has since retracted as no longer reproducing. **Batch 3 was filed at 12:27:11 on 3 August 2026 and `ff03613` landed at 12:27:48**, so the clause was true when written and false thirty-seven seconds later, with no edit to the record and nothing in it to signal the change. The obligation itself never depended on the retracted half: the same `Risk` already named the rate limit, and the architecture's own conclusion is that freshness is bounded by that limit however fast the transfer is. The repair drops the timeout, keeps the limit, and adds the guard the incident earned — a fast download is no reason to move the sync onto the request path — so the stronger-sounding argument is not reinstated later by someone who reads only the record.
- **`NFR-001` cites two sections, because the limits now live in two.** `7bd6152` moved the general one-per-second-per-resource limit out from under *Retrieving zones*, which governs one endpoint, to `Architecture.md § Data sources and constraints`, which governs the API. The record's citation still resolved to a real heading, so no link check could fire; it simply pointed at a section that no longer said the thing. **The part that actually broke is the plural** — the statement read "the rate limits under *Retrieving zones*" and only one limit was left there — which is `@docs-writer`'s observation and worth keeping, because a plural is a load-bearing word that no tooling reads. `Source` now carries both sections, the per-resource criterion re-points to *Data sources and constraints*, and the all-zones interval criterion stays under *Retrieving zones* where that limit still lives. The `Risk`'s back-reference — "the interval that section states" — was correct while `Source` held one entry and ambiguous the moment it held two, so it now names its section outright.

**Batch 3 signed off, 4 August 2026 — all 21 records `draft` → `to-build`.** `FR-019` … `FR-034` and `NFR-001` … `NFR-005`, on the Owner's sign-off brokered by `@engineering-lead`, with the two repairs above applied first: `FR-022`'s repair was a condition of its signature, and `NFR-001`'s became due rather than pending when `7bd6152` landed. **Straight to `to-build` and not through `approved`**, as batches 1 and 2 each were, because `@requirements-reconciler` is dormant and there is no classification step for a record to wait in — no record in this corpus has ever held `approved`. That sign-off left the corpus at 39 records, all `to-build` or later, with `draft` empty — the state it held until batch 3b filed on 6 August 2026.

**Batch 3b filed, 6 August 2026 — `FR-035` … `FR-037`, `draft`.** Four obligations that `ff03613` added to `Architecture.md` **thirty-seven seconds after batch 3 was filed**, all four citing sections batch 3 had already swept. They are **traced, not invented**: the same commit whose retraction forced `FR-022`'s repair also *added* obligations, and a batch that takes the retraction while leaving the additions has read half a diff. Three became records and the fourth is a considered rejection below. No register extension was needed — all three are `Turf data integration`, whose registered scope is already *the fields whose absence or round scope constrains what can be concluded*, and `FR-030` with `FR-031` are the standing precedent for absence-handling records living there. `Zone data fidelity` was weighed for `FR-037` and rejected: that category owns the limits the data's nature places on the cost model and the optimizer, and a region subkey reaches neither.

- **`FR-035` binds the sync's write path, which `FR-022` does not reach.** `FR-022` puts the refresh on a schedule and off the request path; a job that satisfies it perfectly and writes only ids it has not seen before leaves every held zone's `dateCreated` frozen at first sight. The obligation is stated as *the value stored* rather than as an upsert keyed on `id`, because the cited section offers that as one sufficient way to meet the need rather than as the need.
- **`FR-036` is one record and not two, and the pair it was measured against is `FR-030` with `FR-031`.** Those two split because a correctly carried absence can still be misread downstream, so each half is satisfiable while the other fails. Here there is no such independence witness: **rejecting the record is itself a form of treating the absence as a fault**, so the implication runs one way only, which is a specialization and not a branch. The construction that *is* independent — an ingest substituting a value, which escalates nothing yet destroys the absence — **is not in this source** and is owed to the batch scoped to `SPECIFICATION.md § Zone attributes and user-defined value`, in `FR-030`'s shape. Flagged rather than authored, because authoring it here would admit scope through a side door.
- **`FR-037` is `COULD`, and carries a safety guard it was not authored with.** No consumer of a zone's country exists in the first release: `Architecture.md § D5` scopes an OSM *extract* by country, which is a bounding data decision rather than a per-zone attribution, and `SPECIFICATION.md § Geographic scope` argues against a country-keyed gate rather than for one. `COULD` on `FR-025`'s precedent — the path is not excluded, so the obligation binds the moment anything requires a country, and `WON'T-now` would file no story and leave a later aggregate free to be silently short. `Risk` is omitted and its argument folded into `Rationale`, as `FR-025` does. **The guard is the part worth recording.** `SPECIFICATION.md § An unknown speed limit fails the check` forbids resolving a jurisdictional speed-limit tag against national defaults, in these words: the resolved figure *"would rest on a country attribution supplied by the resolver rather than by the data"*. `FR-037` obliges the system to manufacture exactly such an attribution for every zone. It is not a contradiction — the record licenses no use — but read as *the corpus guarantees a country per zone* it is a standing invitation to build the lookup that section forbids absolutely, and the failure would arrive as a plausible limit carrying no sign it had been inferred. The record now bounds its own output to aggregates and names that section.

**The fourth obligation is a considered rejection, and no ID was issued for it.** `Architecture.md § API version` records that `GET /v5` returns **406** to a JSON `Accept` header — content negotiation, a different axis from `FR-019`, which binds which version every request names. It is rejected on **Necessary**. `Architecture.md § Data sources and constraints` and its subsections name every endpoint this system uses and the index path is not among them, so an obligation about how to read a response to a request the system never issues is **vacuous** — and a criterion whose antecedent never fires is the one kind that can never fail, which is the defect `NFR-001`'s amendment was rewritten for. Three constructions were tried and each rejected: *how availability is concluded* invents failure handling that `Architecture.md § Still owed by this document` has not written; *where the JSON `Accept` header is applied* is an `Appropriate` reject, naming an HTTP-client choice to protect an outcome nothing threatens; *what a 406 may not be read as* is the honest residue and is still vacuous. The section's own words give it away — *"worth knowing before that 406 is read as an outage or an auth fault"* addresses a human debugging, not a running system. **The residue lands on the failure-handling debt already recorded below**, and becomes a real obligation the moment failure handling is written such that the system probes the API. **`FR-038` is therefore unissued and `Next free` skips nothing** — allocation is highest-ever-issued plus one, and `FR-037` is the highest.

**Fifth considered zero — the NFR lane, on all four `ff03613` passages, 6 August 2026.** Scoped to those passages and no wider. The strongest candidate was `Estimate accuracy` on the `dateCreated` passage, and it fails on grounds any one of which is sufficient: the obligation is a write path rather than a target; the only honest bar is exact agreement between the stored value and the value the API publishes, which is the functional record restated as *count of X: zero* — the shape batch 3's third de-confliction bullet already ruled functional; the threefold inflation is a measurement, barred from a criterion by override 1; and that category's registered scope is the calibration of **presented** estimates, while the takeover rate is never presented and feeds a multiplier `CalculationSpecification.md § Proposed adjustment` states is a proposed default, so an accuracy bar anchored to it would harden a proposal in breach of override 2. `Coverage and data quality` was declined for the `type` and `region.country` passages on the third zero's own reasoning — that category's trigger is **thin** map or elevation data with confidence falling as it thins, and neither passage is thin data nor moves any confidence. **What would overturn this zero is named:** an accuracy or freshness tolerance for a derived rate, authored into `CalculationSpecification.md`. None exists, and none was invented.

**`Portability` on the country passage was weighed seriously and deferred, not dismissed.** The zones lacking `region.country` are not scattered — Ireland, Spain, Australia, Italy and France head the list — so a country-keyed aggregate does not lose a rounding error, it loses France, which is `SPECIFICATION.md § Geographic scope`'s *"must not be a system that only works in certain countries"* in miniature. It is deferred on the second zero's reasoning: the obligation is created by that section, not by the Architecture paragraph observing a consequence of it, and `SPECIFICATION.md § Non-functional requirements` is already recorded below as the corpus's largest unworked NFR source. Authoring now would let that batch find `Portability` claimed by a record scoped to one Turf subkey. **Recorded as a debt with its home named**, so the owed batch finds this rather than re-deriving it.

**Batch 3b returned zero `human-judgement` records, and the zero is argued rather than accumulated.** The corpus stands at 1 judged record in 42. The look was run per record before any method was chosen, as the skill requires, and what was searched for is recorded so the next reviewer need not repeat it: a maximum permitted age or divergence for a stored `dateCreated`, and any tolerance on the takeover-rate denominator (`CalculationSpecification.md § Takeover rate` states the formula and says outright that its own **minimum-age** guard has no value set; the twelve-month figure under `CalculationSpecification.md § Proposed adjustment` bounds a *zone's age*, not a stored value's freshness, and is proposed besides); a data-completeness threshold, a permitted missing-field rate or an alerting threshold for `FR-036`; and any coverage bar on an aggregate for `FR-037`, where the only aggregate in that document is the **spatial** neighbourhood under `CalculationSpecification.md § The activity baseline`, which cannot be written against a country grouping. **Nothing was found, and nothing needed to be.** This batch is the data plane, and the data plane offers **discipline rather than quality**: update a field, do not escalate an ordinary absence, do not lose a zone from a group. Each has a crisp pass/fail with no standard of judgement in it, and `human-judgement` is the method for a residue after a quality attribute is established — here no quality attribute survived, so the method was never in play. **A judged record forced into this batch would have been a fabricated standard, which is the failure the method exists to prevent, running backwards.**

**Repaired 6 August 2026 — the guard is a minimum-age guard, and there is no zero divisor.** The paragraph above named it a *zero-divisor guard*; found by `@docs-writer` against `5f5f4e7`, which establishes that `dateCreated` carries **second** precision. A zone created minutes ago therefore yields a divisor that is very small rather than zero, the arithmetic returns instead of raising, and **a check for zero can never fire** — so no such guard exists to have a value set. The guard whose value is unset is the **minimum-age** one, and it always was: the old name attached the unset value to the failure's motivation rather than to the guard, which read as loose while the section called the divisor zero and is a false statement about the source now that it does not. **The zero the line supports is untouched.** What the look wanted from that section was a tolerance for the takeover-rate denominator; the section still states none, and a guard with no value set is no more a threshold under one name than the other. Recorded rather than corrected in silence, because a reader meeting the amended section would otherwise have to re-run the look to learn whether the zero had survived it.

**Owed by batch 3b, and deliberately not authored.** Three debts were recorded, each with a stated home. The Owner's sign-off of 6 August 2026 closed the first; two stand.

- **Whether the sync owes a refresh on fields beyond `dateCreated` — closed by ruling, and no record is owed.** The debt was recorded because `Architecture.md § Retrieving zones` observes the same zone's coordinates and `totalTakeovers` moving and warns against a write path that skips fields it assumes constant, while stating the obligation for one field only. The Owner ruled that **`FR-035` stays bound to the field its source names**, that the general obligation is not to be inferred from that warning, and that it already has a home — the same section specifies an upsert keyed on `id` that refreshes the mutable columns, and carries the measurements behind it, so a record restating it would be a second home. Recorded as closed rather than struck out, because a debt that disappears without a verdict reads later as an oversight. **The home the ruling names is not yet the home this bullet can cite, and the difference is timing rather than disagreement.** The ruling names *the schema section*; `Architecture.md § Still owed by this document` lists the schema behind the synced zone table among what that document owes, so no such section is in it as this is written, and a draft carrying one is in flight. The citation above is therefore the resolvable one today — `Architecture.md § Retrieving zones` is where the upsert sentence and its measurements actually live. **When the schema section lands, this bullet's citation re-points to it**, and that repair belongs to the commit that lands it, at the one moment both homes are known. The ruling's effect holds either way: a record restating the general refresh would be a second home whichever section is the first. **Repaired 6 August 2026, by the backstop rather than by the commit.** `e000f20` landed both `Architecture.md § The schema` and `Architecture.md § The sync write path` and carried neither repair, so the interval this bullet was written to prevent is exactly the interval that occurred. The home the ruling named is the second of the two: the upsert keyed on `id` and the columns it refreshes are stated under `Architecture.md § The sync write path`, and that is where a reader challenging `FR-035`'s bound now goes. Found by `@requirements-nfr` while sweeping batch 4 — a lane with no duty here at all, which is the point worth keeping: the primary duty sat with whoever edited the cited file, the citing file gave no signal, and what caught it was an agent reading for another reason.
- **The `FR-030`-shaped companion to `FR-036`**, owed to `SPECIFICATION.md § Zone attributes and user-defined value`.
- **The `Portability` record on country coverage**, owed to `SPECIFICATION.md § Non-functional requirements`, per the deferral above.

**Batch 3b signed off, 6 August 2026 — all three records `draft` → `to-build`.** `FR-035` … `FR-037`, on the Owner's sign-off brokered by `@engineering-lead`. **Straight to `to-build` and not through `approved`**, as all three prior batches were, because `@requirements-reconciler` is dormant and there is no classification step for a record to wait in — no record in this corpus has ever held `approved`. Three rulings came with the signature and each is recorded where it bites: the `FR-038` rejection is **accepted** as argued, with its residue riding the failure-handling debt; **`FR-037` stays `COULD`**, on the ruling that nothing in the first release requires a zone's country, which is the premise its priority already rested on rather than a new one; and the refresh debt is closed by the bullet above. The sign-off leaves the corpus at 42 records, all `to-build` or later, with `draft` empty.

**What no zero covered, recorded so the emptiness above is not over-read.** `SPECIFICATION.md § Genuinely out of reach or out of scope` is cited by `FR-005` and is `FR-012`'s sole source, but it entered batch 1 only as a corroborating second citation and was never swept as a section: five of its six bullets carry no requirement at all, and `FR-012` arrived from an Owner ruling rather than from a batch scoped to it. No NFR question has been put to it, and the absence of one there is **unexamined rather than a considered zero of its own**. That section is owed a batch of its own — an earlier wording of this paragraph described batch 1 as covering "the two source sections behind `FR-001` … `FR-006`", which counted a citation as a sweep and is corrected here.

Owed for the same reason, and noticed while `FR-012` was authored: *The planning player* states that the tool is used in a planning session rather than while driving, "which relaxes how quickly the first answer must appear". A relaxed target is still a target — but the obligation's home is `Architecture.md § Response time and progressive results`, not the section observing it, which is the same reasoning that produced the second zero below. **A `Response time` NFR is owed when a batch scoped to that Architecture section runs**, and is deliberately not allocated here.

**`SPECIFICATION.md § User time constraints` is owed a batch of its own, and this debt is not merely an unswept section.** Batch 2 actively deferred obligations to it: `FR-013`, `FR-017` and `FR-018` each disclaim in their `Rationale` an obligation that section owns — that there must always be at least one journey alternative within the user's stated additional-time limit — naming it as the single definition of the allowance rather than restating it. The consequence has to be written down plainly, because it cannot be seen from the records themselves: **until that batch runs, no record in the corpus obliges the offered set to be non-empty**, and every batch-2 record is vacuously satisfiable by a system that offers nothing at all. Owed to the same batch and deferred for the same reason: the allowance itself, the absolute ceiling over it, the value-based justification a stretch alternative requires, and a stretch alternative's identification as one.

**Batch 4 ran on 6 August 2026 and this debt is narrowed rather than closed.** Every item listed above is now filed: the allowance's capture as `FR-038` … `FR-041`, its allocation across legs as `FR-050` … `FR-053`, the ceiling as `FR-044` … `FR-046`, the stretch justification as `FR-049`, the stretch identification as `FR-047`, and the at-least-one-compliant guarantee as `FR-043`. **What is not closed is the sentence in bold above, and it is worth being exact about how much of it survives.** Two of the three ways the offered set could be empty are now shut: a system that produces a compliant alternative and offers only above-limit ones violates `FR-043`, and one that returns nothing *because the budget is small* violates `FR-042`. The third stands — **nothing yet obliges the system to produce an alternative at all**, so a search that simply yields nothing still satisfies every record in the corpus. That residue is not a gap batch 4 left; it is the second-hole debt below, arrived at from the other direction, and its home is `Route alternative generation`, which this batch opened as a file. `FR-043` is additionally the **conditional** form of its guarantee, and the question it was conditioned on is now ruled: the Owner ruled on 6 August 2026 that the guaranteed alternative must capture at least one zone, so a journey carrying no Turf stop does not discharge it. **The conditional form survives the ruling and no unconditional record is owed**, because the two conditions were never the same one — the record is conditioned on *production*, which the ruling does not reach, and it was the *content* of the guaranteed alternative that the ruling settled. What discharges the production condition is the second-hole debt below and nothing else.

**A second hole of the same shape, and it is not the one above.** Found by `@requirements-story-organizer` while cutting batch 2's stories, and recorded here because it cannot be seen from any record on its own. The debt above is that the offered set may be **empty**; this one is that it may be **singular**. Every batch-2 criterion is conditioned on alternatives *having been produced* — `FR-013`'s first criterion opens *given a journey for which more than one alternative … has been produced* — and **nothing anywhere in the corpus obliges the system to produce more than one alternative in the first place**. A search that only ever yields one satisfies `FR-013` without ever putting a choice in front of a user, which is the whole of what that record exists to secure.

The obligation's home is `Route alternative generation`, **which batch 4 opened as a file on 6 August 2026 while this debt stayed open.** `FR-042` is its first and only inhabitant, and it is a different obligation — that the search does not decline *by reason of the size of the stated limit*, which says nothing about how many alternatives it yields when it does run. The debt is therefore unchanged in substance and cheaper to discharge: the batch that takes it now files into an existing category rather than opening one, and the register's scope line was widened in the same pass to say outright that this category owns whether alternatives are produced at all and in what number — the reading this debt had already assumed and the register had never carried. **It is owed to the batch scoped there**, and it is deliberately not authored now — writing it here would be admitting scope through the side door that story creation opened, and the register entry exists precisely so it is not. Recorded rather than storied, and carried on `#12` as an openly-stated hole so no worker reads that story as guaranteeing a plural result.

**Owed by batch 3, and deliberately not authored.** Four debts, each with a stated home, so none of them is later mistaken for an oversight.

**The honesty residue of confidence against data age.** A `human-judgement` record was authored — that a recommendation built from ageing Turf data must not present a confidence overstating how current it is — and is **held unfiled pending an Owner ruling**, not rejected. `SPECIFICATION.md § Confidence and uncertainty` enumerates ten uncertainty drivers and every one is a source of error in the *travel-time estimate*; data age is not among them, and stale ownership does not make a time estimate wrong, it makes a *value* estimate wrong. So the open question is not whether that enumeration is incomplete but whether it is the right standard at all. `NFR-002` carries the machine-checkable relation; the residue is owed to the batch scoped to `SPECIFICATION.md § Confidence and uncertainty`.

**Owed to the `Call budget` batch** — `Architecture.md § The call budget`, batch 4: the per-journey Turf call bound; the one-call properties of held-zone and region-lord retrieval; and the over-tiling concern in *Retrieving zones* — splitting a corridor into tiles far below the permitted area — which is a call-volume claim with no threshold that was not invented.

**Owed to failure handling, which `Architecture.md § Still owed by this document` has not written:** what the local copy is, and what may be concluded from it, when a sync fails or completes partially. **The debt stands; its original ground does not.** This entry was written against a partial download that timed out, recorded in `Architecture.md § Retrieving zones` as an observed fact — and that section has since retracted the observation as no longer reproducing. What the retraction removes is the *instance*, not the *hole*: a sync can still fail or complete partially for reasons the timeout was only one of, and neither that section nor any other states an obligation about the result. The ground is now the silence rather than the incident, which is the stronger of the two and should have been the ground from the start.

**`SPECIFICATION.md § Non-functional requirements` is an entire unswept section**, holding *Platform and mobile-first design*, *Geographic scope*, *Languages*, *Estimate accuracy and calibration* and *Time of day and traffic*. It is the corpus's largest unworked NFR source and it is owed a batch of its own.

**Why the second zero, recorded so it is not re-litigated.** That section names the one-home-per-model invariant while complying with it — the scoring model is defined once, in `CalculationSpecification.md`, and not restated. The obligation is created by `docs/README.md § Conventions`, not by the section observing it, so an NFR sourced from the observation would cite the wrong home; and narrowing it to the scoring model alone would shard a corpus-wide invariant per occurrence, giving the one-home rule more than one home — the exact defect it exists to prevent. **One corpus-wide `Maintainability` NFR, verified by `inspection`, is owed when a batch scoped to `docs/README.md § Conventions` runs.** It is deliberately not allocated here.

**Third considered zero — `SPECIFICATION.md § Recommended journey alternatives`, 2 August 2026.** The section states one obligation of its own — curate the offered set, avoiding effective duplicates and alternatives with no meaningful trade-off — and everything quality-*sounding* in it is the standard by which that one duty is judged, not a second duty. "Meaningfully different", "effectively duplicates" and "meaningful trade-off" are the adverbs of the curation rule; the corpus's home for a standard of judgement is the `Verification` field of the record carrying the duty, never a companion record. An NFR restating them would be the same sentence filed twice, split only by verification method, and would collide head-on with the FR sweep of this same section. One obligation, one home.

No threshold exists to hang a metric on. "Several" is not a count, and nothing in `CalculationSpecification.md`, `Architecture.md` or `DESIGN.md` sets a number of alternatives to present or a distinctness bar between them. The nearest thing in the corpus — "a route alternative is not merely a variation caused by visiting one additional zone", `SPECIFICATION.md § General route alternatives` — is a *definition* rather than a threshold, and it governs the general-road-route layer, not the Turf-strategy layer this section governs. Any number filed here would have been invented.

The fallback is not `Coverage and data quality`, and this is the trap the section sets. That category degrades on **thin map or elevation data**, with confidence falling as it thins. This section degrades on **zone availability** — there genuinely is no attributed zone inside the user's limit — which is a fact about the world, not a deficiency in our data; the system knows it perfectly well and its confidence is unaffected. Filing it under that category would give "graceful degradation" two triggers and leave the later data-thinness batch working around a record that had already claimed the name. What the fallback obliges is an action in a branch, with an unhappy path and no quality target attached. That is functional, and it is `FR-018`.

The allowance, the absolute ceiling over it and the at-least-one-compliant guarantee are all sourced from `SPECIFICATION.md § User time constraints`, which states it is their single definition; they are owed to a batch scoped there. No `Response time` question was put to this section — that NFR is already owed to `Architecture.md § Response time and progressive results` and is not pre-empted here. An `Observability` record on how often the fallback fires was considered and rejected: no document asks for it, and authoring it would be inventing scope rather than reading it.

**Batch 4 filed, 6 August 2026 — `FR-038` … `FR-054`, filed `draft` and signed off the same day.** Seventeen records from `SPECIFICATION.md § User time constraints` and its subsection `SPECIFICATION.md § Journeys with several legs`, the section three batch-2 records defer to by name. It is a **safety path** — the absolute ceiling lives here — and every record touching the ceiling was authored and reviewed as one. Five categories carry it: `Journey initialization` (`FR-038` … `FR-041`), `Route alternative generation` (`FR-042`), `Recommendation set composition` (`FR-043`, `FR-049`), `Safety exclusions` (`FR-044` … `FR-046`, `FR-053`), `Recommendation disclosure` (`FR-047`, `FR-048`, `FR-054`) and `Multi-leg budget allocation` (`FR-050` … `FR-052`) — two of them registered in this pass and three opened as files, with the reasoning above the register table rather than here.

**No constant entered the corpus, and this section is the one where that was hardest.** The multiplier setting the ceiling is a **proposed default with nothing measured behind it**, and a record hardening it into a `MUST` would have read as the *better* record — more precise, more testable — until the day the figure is measured. `FR-044`, `FR-045` and `FR-046` therefore oblige, respectively, the base the ceiling is derived from, that no alternative above it is offered whatever its value, and that a configuration raising the multiplier is refused. None of the three states a figure; a digit scan across all seventeen records returns exactly one, in a date. **`FR-046` is the record to read if this rule is ever argued about**: it binds the *strict direction* of a configurable constant without binding the constant, which is what override 2 asks for and what is easy to mistake for pedantry until the value moves.

**Sixth considered zero — the NFR lane, on `SPECIFICATION.md § User time constraints`, 6 August 2026.** Scoped to that section and its subsection. All thirteen registered quality attributes were walked; nine were empty on inspection and four argued. The strongest candidate by far was **result quality across the budget range** — the section's claim that the optimizer behaves sensibly across the whole of it — and the lane took it as far as `human-judgement` before rejecting it, on a distinction worth preserving: **an absent threshold licenses the judged method, an absent *standard* forbids it.** A judge asked whether the answer at a small budget is as good as achievable would be inventing the bar in the act of applying it, which is a fabricated standard wearing the method that exists to prevent one. The high end of the same sentence fails separately — what a large budget must not degenerate into is owned by `SPECIFICATION.md § Individual zones rather than local collection routes`, an unswept section, and restating it would claim a name an owed batch needs. `Estimate accuracy` was deferred to `SPECIFICATION.md § Estimate accuracy and calibration` rather than authored, and `Maintainability` failed on the second zero's exact shape — a section naming the one-home invariant while complying with it. **What would overturn this zero is named:** a usefulness or minimum-result bar authored into `CalculationSpecification.md`, or an Owner ruling that both guarantees an answer at the low end **and supplies the standard by which it is judged useful**. The ruling alone would not be enough, and that is the part to remember.

**The batch's verification split is 16 `test` and 1 `human-judgement`, and the shape of it is the argument.** A time allowance is arithmetic — a base, a multiplier, a sum across legs, a comparison — so a criterion can be written against a **relation** without any threshold at all, and four records exploit exactly that: `FR-044` compares two ceilings, `FR-053` compares a sum against a limit, `FR-051` compares two ratios, `FR-052` is bounded by what a leg left behind. None needed a number and none invented one. The single judged record is `FR-049`, and it is judged for the one clause in the section that is not arithmetic: an above-limit alternative is permitted only where the additional Turf value is a **clear justification**. The look found a *form* and no *bar* — `CalculationSpecification.md § The objective function` and `CalculationSpecification.md § Proposed form: value per minute` give a comparison and set no floor under it, `CalculationSpecification.md § Zone value` weights attributes without a bar for justifying extra time, and the ceiling section bounds time while saying nothing about value. A figure standing in for *clear* would have been chosen on the spot, would then be measured, would pass, and would leave the quality it stood for unexamined. `FR-015` is the machine-checkable floor beneath it and is cited rather than restated. The corpus moves from 1 judged record in 42 to 2 in 59, and `FR-055` … `FR-057` leave the numerator alone while moving the denominator to 62: all three are `test`, and each is a presence, a sign or a bound on a value the user typed, with a crisp pass/fail and no standard of judgement in it.

**Batch 4 signed off, 6 August 2026 — all 17 records `draft` → `to-build`.** `FR-038` … `FR-054`, on the Owner's sign-off brokered by `@engineering-lead`. **Straight to `to-build` and not through `approved`**, as all four prior batches were, because `@requirements-reconciler` is dormant and there is no classification step for a record to wait in — no record in this corpus has ever held `approved`. The sign-off leaves the corpus at 59 records `to-build` or later. **It is the first sign-off this corpus has recorded with its story pass still outstanding.** Every batch has an interval between its signature and the story pass that follows, story creation being a pass of its own; batch 4's was the first written into the corpus and committed in that state, because it was the first not to close inside the commit that opened it. While it stood, `Resolved-by: —` on a batch-4 record meant *signed off, story pass due*, where before that day every `—` in the corpus meant *not yet signed off*. **The pass has since run and that second reading retired with it** — the three `—` it left behind were `FR-055` … `FR-057`, meaning *not yet signed off*, and those went too when the three were signed and storied in a single pass on 7 August 2026. No `—` stands anywhere in the corpus today.

**Story pass run, 6 August 2026 — four epics and seventeen stories, `#41` … `#57`.** `@requirements-story-organizer` cut Milestones 10 to 13 — *The additional time the user agreed to spend*, *The absolute ceiling on added time*, *The stretch band, and what it must disclose*, and *Journeys with several legs* — and one story per record, no record taking two stories and no story resolving two records. The links live in `TRACEABILITY.md`, transcribed in the *Story → requirement* direction from each issue's own `Resolves:` line, and the views of them on the records and in `INDEX.md` were written in the same librarian pass. **A story's number and its epic's Milestone name are the whole of what the corpus holds about either**, per `requirements-authoring § Corpus layout`: neither the board column any of the seventeen sits in nor the `human-verified` label `#57` carries appears anywhere in this folder, because the board and the issue answer both live and a copy here would be stale from the moment it was written.

**None of the seventeen joined an existing epic, and that is `@requirements-engineer`'s decision.** `FR-043` and `FR-049` are the case that needed deciding: they are `Recommendation set composition` records whose six neighbours in that category — `FR-013` … `FR-018` — all sit under *A real choice of journey alternatives*, which made that epic the obvious home for them. It was refused deliberately. **That epic's records are limit-blind**, every one of them buildable with no part of the allowance model in place, and folding in two records that presuppose the stated limit and the band above it would invert a dependency that runs the other way. It would also cost that epic the property that makes it schedulable on its own, which is a large price for keeping a category's records together in a structure that was never organized by category. The other fifteen had no existing candidate at all — the allowance model is new ground for the corpus — so the four new Milestones follow the source section's own structure, the limit and the ceiling and the stretch band and the multi-leg case, rather than the category files'.

**Three rulings came with the signature, and each produced work.** They are recorded here with what each changed, because a ruling whose effect is only visible as a diff is one the next reader has to reconstruct.

- **Ruling 1 — the guaranteed alternative must capture at least one zone.** This takes `FR-043` off the conditional form it was authored in for a *different* reason than the one that form was chosen to hedge. The record's heading, `Statement`, `Verification`, `Acceptance`, `Risk` and `Rationale` were all revised by `@requirements-engineer` before signature, and the record is now `FR-043` — *Offer a zone-capturing within-limit alternative where one was produced*. The defeat the ruling forecloses is the quiet one: a journey carrying no Turf stop adds no time, is therefore always within the limit, and offering it satisfies the guarantee's letter while handing the user the drive they already had. The production condition is untouched and still stands on the second-hole debt.
- **Ruling 2 — a pre-filled default is not a user-supplied limit, and the field starts empty.** This closes the open question `FR-038` returned; that record's `Rationale` was revised to say so and to point at the record carrying the new obligation, and `FR-055` was filed for the obligation itself. **It is a separate record and not an extension of `FR-038`** for the reason the record states: the two are testable at different moments — `FR-038` fires when planning is attempted, `FR-055` when the planner opens — and `FR-055` is what makes `FR-038`'s criteria decidable at all, since those criteria turn on whether the user entered a limit and an untouched default can be read either way.
- **Ruling 3 — a limit of zero or less is refused, and no maximum is imposed.** Two halves, two records, because the outcomes are opposite and separately testable: `FR-056` obliges a refusal and `FR-057` forbids one. `FR-056` is not `FR-040`'s, which admits any value *within* the realistic range and creates no bound of its own, and is not `FR-038`'s, where the user entered nothing at all. `FR-057` is not `FR-040`'s either, and the gap is the point: a maximum placed just above the realistic range satisfies `FR-040` while refusing a user the section calls ordinary.

**`FR-055` … `FR-057` were filed `draft` on 6 August 2026, and that was deliberate rather than an omission.** The Owner had ruled the substance of all three; the wording was `@requirements-fr`'s and unsigned, and `@requirements-engineer` does not sign records it commissioned, so they waited for the Owner exactly as any authored record does — the ruling settles what the corpus must say, never that a particular sentence says it. The signature came on 7 August 2026, recorded below. **No constant entered with them and none was needed**: positivity is a property of a quantity of time rather than a value that could have been chosen otherwise, and the absence of a maximum is the absence of a figure rather than a figure of zero. **No register extension was needed either** — all three are `Journey initialization`, whose registered scope is already *input capture, the time budget, validation, session start*, and all three file into `journey-initialization.md` in ID order after `FR-041`. They are the first records this corpus has filed that exist because of a ruling on an earlier record rather than because of a source section swept for the first time, and the register absorbed them without amendment, which is a point in the register's favour rather than a gap in it.

**`FR-055` … `FR-057` signed off, 7 August 2026 — all three `draft` → `to-build`.** On the Owner's sign-off. **Straight to `to-build` and not through `approved`**, as all five prior batches were, because `@requirements-reconciler` is dormant and there is no classification step for a record to wait in — no record in this corpus has ever held `approved`. The sign-off leaves the corpus at 62 records, all `to-build`, with `draft` empty. `@requirements-story-organizer` cut `#58` … `#60` for them the same day, under *The additional time the user agreed to spend* rather than a new Milestone. **The epic is `@requirements-engineer`'s decision**, on three grounds: all three are the initial state and the two edges of the input that Milestone already covers; each depends on a record already inside it — `FR-055` → `FR-038`, `FR-056` → `FR-038`, `FR-057` → `FR-040`; and a separate epic would split *what a valid additional-time limit is* across two Milestones. This is the second allocation to cut no new Milestone, after batch 3b's.

**`FR-055` was amended twice on 7 August 2026, and the second of the two is the first edit this corpus has made to a record after it was signed.** Recorded because a diff alone would not say why, and because the two amendments are different events with different standing. **They are also very unequal in size, and this paragraph had them the wrong way round when first written**: the pre-signature amendment is much the larger of the two and is where all three of the new citations arrived. Corrected against the working tree by `@requirements-engineer` the same day.

The first came **between authoring and sign-off**, on a further Owner ruling that arrived in the interval: a limit restored from an earlier session is pre-filled, so the field must start empty whatever the value's provenance. The record already carried that ruling's substance — its **second** criterion named one source of a restored value — and the amendment widened that criterion off the named source and onto **retention**, with the second half of `Verification` moving with it. `Rationale` gained the provenance argument and, commissioned in the same pass, the whole stored-plan scope note: a stored route keeps the limit it was approved against, so reopening one is not the planner opening — cited to `SPECIFICATION.md § Route persistence`, `DESIGN.md § Returning to a stored plan` and `DESIGN.md § Never gate stored plans on the wizard`, and stated outright as scope rather than an obligation about what a reopened plan shows. **All three citations arrived here, pre-signature**, and each was re-resolved against its cited document's current headings in that pass. That is an amendment in `draft`, of the ordinary kind: no signed record moved, no story existed, and no index row was stale for an instant.

The second came **after the signature**, and it closed a gap the first had left rather than a seam it had opened. Widening the second criterion onto retention while the first still turned on **planning history** left the pair on two axes, and two givens on two axes leave a device state matched by neither. The amendment re-framed the **first** criterion onto retention as well, moving the first half of `Verification` with it, so the two now sit on one axis as a predicate and its negation and cover every state. `Rationale` gained exactly one sentence saying so. That is the whole of it: one given re-framed, one clause of `Verification`, one sentence.

**The obligation never moved, and that is the whole of why the second amendment did not need a second signature.** The `Statement` is untouched, and so are the title, the `Category`, the `Priority`, the `Verification` keyword and every citation the record carries. **An amendment that re-frames one criterion onto the axis the record's other criterion already carries is not a re-opening of a signed obligation** — the distinction to keep is that a record whose *statement* moves after signature is a different event and goes back to the Owner. It is also **not the rot class** that the batch-3 repairs and `FR-036`'s discharge recorded: nothing went stale, no citation stopped resolving, and no reader was ever told something false. **The size of it is the argument rather than a mitigation of it**: a reader diffing the signature against today finds one given re-framed and one sentence added, and can see the obligation standing untouched between them.

**Ruling 3's no-maximum half will attract a safety question, and the Owner's reason is recorded so a reviewer finds it rather than reopening it.** `FR-057` forbids a bound on an input on a batch that is a safety path, which reads at first glance like a safety rule removed. It is not. **Any maximum would be an invented literal** — nothing in `CalculationSpecification.md` states one, and a figure picked to sit above *several hours* would be a constant with no home, hardening into a `MUST` exactly as override 2 forbids. And **the work is already bounded elsewhere**: the corridor half-width has its cap under `CalculationSpecification.md § Bounding the candidate set` and the candidate set has its own, so a large limit buys a wider search within bounds that already exist rather than an unbounded one. What a large limit must not do is degenerate into an urban collection route, and that is not this record's — it is the debt owed to `SPECIFICATION.md § Individual zones rather than local collection routes` below. The ceiling is untouched by all of this: `FR-044` derives it from whatever limit was stated and `FR-045` enforces it absolutely, so a larger stated limit moves the ceiling with it rather than past it.

**One ambiguity was worked around rather than resolved, and it is with the Owner.** *The optimizer prioritizes alternatives that respect it* has three readings — search priority, which is unobservable; set membership, which is `FR-043`; and presentation order, which would collide with the Owner's ruling of 1 August 2026 recorded on `FR-010`. Set membership was taken and **no ordering obligation is authored anywhere in this batch**. **This paragraph carried a second ambiguity until 6 August 2026** and ruling 1 settled it: whether a journey carrying no Turf stop discharges the at-least-one guarantee. It does not. The half is struck rather than left standing, because an ambiguity recorded as open is an instruction to the next author to work around it.

**The `FR-043` / `FR-016` precedence was raised and ruled to have no instance.** `@requirements-fr` authored a clause giving `FR-043` precedence where the two cannot hold together, and referred it up rather than filing it silently. It was removed. `FR-016` obliges a set in which each alternative gives a distinct reason to choose it, and an alternative within the limit the user themselves stated carries the most legible reason in the set — so removing the last compliant alternative makes the set worse by that record's own standard, not better, and the collision has no honest instance. **`FR-013`'s precedence rule for its own floor is left undisturbed**, which is the part the removed clause would have quietly reversed. A precedence rule written for a case that cannot occur is one a later reader applies to a case it was never written for.

**Discharged — `FR-036`'s falsified premise is repaired, 6 August 2026.** Found by `@requirements-librarian`'s backstop, the second instance of the class the batch-3 repairs recorded and the first to land on a record that was already **signed off**. The clause read *"that schema is listed as still owed under `Architecture.md § Still owed by this document`, so a record naming a column would bind a decision nobody has taken"*. The heading still exists, so no link check could fire; the section simply no longer said it — the schema left that list when `e000f20` landed. **The repair takes the second of the two readings the finding offered**: not *the premise was wrong* but *the conclusion now rests on a better premise*. `Architecture.md § The zone table` now declares the synced copy's columns, `type_id` among them, nullable, so the clause reads that a record naming the column would give a decision **already taken** a second home rather than pre-empt one not yet taken — and it adds that `Architecture.md § The schema` marks that DDL a proposal, which sharpens the argument rather than blunting it, since a record naming the column would then harden a proposal into an obligation. **The obligation is untouched**: the statement, the criteria, the priority and the status all stand, and only `Rationale` moved. It was routed to the Owner through `@engineering-lead` rather than quietly amended, because choosing between the two readings is a content judgement on a signed record and it should not have ridden a batch-4 commit unremarked. **The class is what to keep, not the instance**: two of these have now been found by the backstop and none by the commit that caused them, which is the interval `docs/README.md § Conventions` says the backstop exists to shorten and cannot close.

**`FR-046` rested on a contested reading of its own source, and the reading is now confirmed.** The record obliges refusal of a configuration setting the ceiling multiplier **above** the stated maximum while leaving a stricter local value permitted, and `CalculationSpecification.md § The absolute additional-time ceiling` appeared to contradict itself on exactly that point: a deployment may lower the multiplier, yet revising it *"is not a deployment setting"*. `@docs-writer` examined the passage and `77998cc` landed the clarification on 6 August 2026, in favour of the reading `FR-046` was authored against — **the maximum permitted value moves only by ratification, and the value a deployment enforces is a separate object that may be stricter and revises nothing.** No repair is due. Both clauses `FR-046`'s `Rationale` attributes to that section — that the multiplier's strict direction is downward, and that a configuration setting it higher is invalid rather than merely unusual — are still there verbatim, and the section's heading is unchanged. **Recorded even though it landed clean**, so that a reader meeting the record does not re-run the examination, and so that the shape is on file: had the clarification gone the other way it would have been a citation-and-clause repair on a signed record, not a re-litigation of the obligation.

**The `recommendation-set-composition.md` migration is done, on the commit it was twice deferred to.** It was eligible on the filing pass and declined, eligible again on the sign-off pass and declined again — both times because the edit making it eligible was a content edit, and a content diff is precisely the diff a citation migration must not sit beside, since in a diff a re-worded citation and a re-worded obligation are indistinguishable. The named moment was the librarian-only commit writing `Resolved-by`, and it arrived on schedule: `FR-043` and `FR-049` take their story numbers here, that is the file's other reason to be edited, and there is no content anywhere in the commit to be confused with the reformat. **The file converted whole** — four old-form citations to the token, in `FR-017`'s `Statement`, `FR-018`'s `Statement` and two in `FR-018`'s `Rationale` — and each was re-resolved against its cited document's current headings rather than merely re-spelled: `SPECIFICATION.md § Recommended journey alternatives`, `SPECIFICATION.md § User time constraints`, `SPECIFICATION.md § Optimization objectives` and `CalculationSpecification.md § The objective function` all exist and all still hold what the citing line attributes to them. The file now holds zero old-form citations, zero italic spans carrying a section marker, and twenty-three token-form citations; its twelve remaining italic spans are quotations of the source's own wording and decide as emphasis by the marker rule. **`Source` and `Depends-on` were not touched**, as `requirements-authoring § IDs and citations` requires — the whole value is the citation there and takes no delimiter — and the corpus still holds zero delimited lines in either field. `docs/README.md § Conventions`'s converted-file list gains the file in this same commit.

**`turf-data-integration.md` became eligible on the sign-off pass and was declined on the same reasoning.** `FR-036`'s discharge was an edit to that file and therefore an eligibility trigger, and it was a content edit — one clause of `Rationale`, replaced on a judgement between two readings. Recorded so a later pass does not re-derive the decision: **the trigger and the disqualifier were the same edit**, which is the ordinary case rather than bad luck, since the edits that touch a category file are mostly content. A file whose only edits are content edits converts on a librarian-only pass or not at all. **It was not eligible on the 6 August story-link pass and was not in that commit at all**: every batch-3b record already carried its story, so the `Resolved-by` write reached none of its records and the file went untouched. The same holds on 7 August 2026, and for the same reason.

**No file became eligible for the migration on 7 August 2026, and the test did not need running.** That pass edits four files. `journey-initialization.md` is the only category file among them, and it is **already on the converted-file list**, admitted born token-form — so the librarian-only `Resolved-by` write that would have qualified as its other reason has no conversion to carry. The other three are `README.md`, `INDEX.md` and `TRACEABILITY.md`, which `docs/README.md § Conventions` places in the class that is **permanently ineligible**: a citation there is repaired rather than migrated, and only a migration puts a file on the list. Recorded because an empty result and an unrun test look identical afterwards, and this was the second.

**Old-form counts, recounted rather than transcribed — and three of the figures have needed correcting.** `turf-data-integration` 10, `cost-and-time-composition` 6, `objective-selection-and-ranking` **5**, `zone-data-fidelity` 4, `runtime-and-deployment-shape` **4**, `journey-definition` 1, `outbound-rate-compliance` **2**, `value-model` 1 — **33 in total**, `recommendation-set-composition` having dropped out of the list at zero. **The first correction is `runtime-and-deployment-shape`'s**, found on the 6 August 2026 recount and carried as 3 since 4 August 2026: its fourth old-form citation is **split across a line break**, `NFR-003`'s `Rationale` reading *D6* at the end of one line and `Architecture.md` at the start of the next, so every count taken with a per-line pattern missed it. That is the split-citation failure the atomicity rule exists to prevent, in the old form and behaving exactly as the rule predicts — it reads perfectly as prose and cannot be found by `grep`, which is why three passes counted past it. **It is left in place deliberately.** Repairing it would edit a file the commit that found it had no other reason to touch, and the repair it actually wants is the conversion, due the next time that file is edited for a reason of its own.

**The other two corrections were found on 7 August 2026, and they hid the other way round.** `@requirements-librarian` found a **bare back-reference** in each — `NFR-001`'s `Statement` in `outbound-rate-compliance.md` reads *Retrieving zones* in the same document, and `FR-010`'s `Rationale` in `objective-selection-and-ranking.md` reads *User time constraints* in the same document — and a back-reference names **no document at all**, so a count pairing an italic span with a filename after it passes straight over one. The split citation above was invisible because it was broken across a line; these two are invisible because they are short a filename, and both defeat the one property the token form was chosen to give this corpus — a citation a pattern can find. `outbound-rate-compliance` moves from 1 to **2** and `objective-selection-and-ranking` from 4 to **5**, the total from 31 to **33**, and the pre-conversion total from the 35 that recount reported to **37**. **The two italic spans this paragraph quotes are quotations and not citations**, and a sweep meeting them here should read them as the evidence rather than as two more instances; `README.md` is in the permanently ineligible class either way. **Both citations are left in place** on `@requirements-engineer`'s instruction: a citation missing its document is a librarian finding on its own account and is being taken up as one, and repairing either here would put a content edit into a commit with no other business in either file. **Only the counts moved, and the counts live in this file** — which is the whole reason the correction is clean, since a derived count is fixed in the view and never by editing the records it counts.

**The rule gap the five new files exposed is closed, and they are on the list.** They were born entirely in token form, yet `docs/README.md § Conventions` admitted a file to the converted-file list only by **migration**, so a file that never held the old form could never join — the cheap syntactic check permanently unavailable to the corpus's newest and cleanest files, the list empty, and every file still paying for the heading lookup. Raised by `@requirements-engineer` with a proposed default rather than patched, and **ratified by the Owner on 6 August 2026**: a file whose citations are token-form from its first commit joins the list in that same commit, declared in the commit message. `journey-initialization.md`, `multi-leg-budget-allocation.md`, `recommendation-disclosure.md`, `route-alternative-generation.md` and `safety-exclusions.md` are now listed, admitted on a check that was actually run — zero old-form citations and zero italic spans in each — one commit later than the new route would require, which that convention records as an exception and not a precedent.

**`FR-057` puts the first italic span into a file on that list, and it is decidable.** Its `Rationale` reads *several hours*, quoting the range's high end from `SPECIFICATION.md § User time constraints` as emphasis rather than citing anything. The syntactic check the list licenses turns on one property, stated under `docs/README.md § Conventions`: a delimited span carrying the section marker is a citation, and one without it is emphasis. This span carries no marker, so it decides as emphasis by the rule's own terms. It decides a second way as well: inside a record's fenced field block the citation delimiter is the **code span**, per `docs/README.md § Conventions`, so an italic span there is not in the citation delimiter class at all. **What the list guarantees is untouched** — every citation in these five files is still token-form. Recorded because the admission evidence for `journey-initialization.md` was *zero old-form citations and zero italic spans*, and the second half of that is no longer true of the file: a reviewer re-running the admission check will now meet a span, and should read this rather than a drift.

**Two judgements recorded so they are not re-raised.** `FR-041`, `FR-051`, `FR-052`, `FR-054` and now `FR-057` are `SHOULD` and carry no `Risk`, which `@requirements-librarian` queried against this batch being a safety path. They are correct as filed: the field is required on `MUST` and safety-path records, and none of the five is on a safety path in the sense `safety-path-checklist` defines — none decides access, stop position, a routing exclusion, or the ceiling, and none feeds a constant that does. `FR-054` is the one worth the second look, since it presents the very quantity `FR-053` tests, but showing a per-leg figure where a total belongs misrepresents a time commitment rather than sending a driver anywhere; the ceiling is enforced by `FR-053` either way. `FR-057` is the fifth instance and the query is not re-run for it: it forbids a bound on an input, and the ceiling derived from that input is `FR-044`'s and `FR-045`'s, which are `MUST` and carry their `Risk`. **Five instances is enough to state the shape rather than the list** — a `SHOULD` that forbids an addition, on a batch whose safety obligations are carried by neighbouring `MUST` records, is the pattern, and a sixth needs no separate paragraph. Separately, `journey-definition.md`'s scope line prefixes the register's wording with *What a journey is:* where every other file copies it verbatim. Left alone deliberately — the divergence is cosmetic, and repairing it would make that file eligible for the migration declined above, which is a large consequence for a colon.

**Owed by batch 4, and deliberately not authored.** Five debts, each with its home named.

- **The budget-size-is-not-an-exception clause**, owed to the batch scoped to `SPECIFICATION.md § Individual zones rather than local collection routes`. `@requirements-fr` tried three constructions for *a three-hour budget must not degenerate into an urban collection route* and rejected each: that a large limit must not admit a zone a small one would refuse is simply **false**, since affording more zones is what a budget does; that admission criteria are limit-independent is a **Correct** reject, contradicted by that section's own fifth admission bullet, which scales with available time; and a bare prohibition on producing a collection route restates the other section and needs a threshold — many zones along a corridor against a collection route — that exists nowhere. The clause is that section's to absorb, and it should absorb it explicitly.
- **The uncertain-stop bound and the refusal of an acceptance that would breach the ceiling**, owed to `SPECIFICATION.md § Reconciling this with the absolute ceiling`. It is ceiling behaviour and will file under `Safety exclusions` beside `FR-044` … `FR-046`, so it is worth scheduling near this batch rather than by document order.
- **The re-check of the budget after every change during review**, owed to `SPECIFICATION.md § Consequences for the optimizer`. `FR-045` enforces the ceiling on what is offered; nothing yet enforces it on what review substitutes in.
- **That an ambitious objective must never silently relax the target or the ceiling**, owed to `SPECIFICATION.md § Interaction with the time budget`. Noted here because it reads as a ceiling obligation and is not one — the duty is on objective selection, and pre-empting it from this batch would have given it a home in the wrong subsystem.
- **A `Response time` condition naming the budget range.** `Architecture.md § Response time and progressive results` sets its targets and names no input range, while this section establishes a wide one and the corridor half-width scales with the allowance up to its cap. The owed `Response time` record needs the envelope in its **condition**, or it is satisfiable by a measurement taken at a mid-range budget. Recorded against the debt already standing for that Architecture section rather than authored here.

**Batch 5 filed, 7 August 2026 — `FR-058` … `FR-093`, `NFR-006` and `NFR-007`, all `draft`.** Thirty-eight records, the largest batch this corpus has taken, and the first run under the clustering and reconnaissance rules ratified the same day. The cluster is **access**: `SPECIFICATION.md § General route alternatives`, `§ Candidate zone identification` with its two subsections, `§ Terrain, elevation, and topographical accessibility` with all eight of its subsections, `§ Accessibility principle`, and `§ Route construction and optimization behaviour` with its subsection. Five category files were opened. Four of the five names were already on the register and had stood `reserved` since 1 August 2026 — `Candidate identification`, `Access classification`, `Coverage and data quality` and `Classification correctness` — which is the register's *permission to file, not a promise that requirements exist* discharging exactly as written. `Route construction` was registered in this pass, with its reasoning above the register table.

**The batch ran long, and the overrun is the rule working rather than failing.** The cap is near thirty and the batch is thirty-eight. The clustering rule states that where the cluster and the cap disagree the cluster wins, because splitting a cluster reintroduces the deferral the clustering exists to remove. Reconnaissance predicted about twenty-seven; thirty-one were authored, two came from the non-functional lane, and five more were added by the safety remand below. **Where the prediction failed is worth recording, because it was not in the sections' content.** It was in how many records an access lattice needs before a candidate cannot reach a confident class by a path nobody wrote a gate for — the gate records outnumber the records stating what the classes are.

**The reconnaissance-first rule paid, and the measurement is not the question count but the silence after it.** Twenty questions went up with proposed defaults before a record existed and were answered in one round; the two authoring lanes then returned **zero** questions the settled ground had not already covered, and no record was written twice. What the rule did **not** prevent is the safety remand below, and that is the honest bound on what it buys: reconnaissance settles what the *documents* leave open. It cannot find what a set of records does to each other, and it cannot find a hole that only exists once a record closes the lattice around it.

**The set-level consistency pass ran for the first time, on the cluster it was specified for, and returned two findings.** The first is a straight contradiction between two records in the same batch: `FR-074`'s third criterion priced a candidate with no connected path by the straight-line fallback, while `FR-076`'s first criterion excluded that same candidate. Both read correctly alone, both cite their sources faithfully, and the sources disagree — `CalculationSpecification.md § Flat-distance fallback` opens *"Where no connected path **or** elevation profile can be obtained"* while the flowchart under `CalculationSpecification.md § Stop time` sends an absent or disconnected path to **Excluded** and only the residue to the fallback. This is the shape the pass was written to catch and it is the one nothing else would have: no per-record check fires, because neither record is wrong. The second finding is a duplicate obligation across the two lanes — the confidence-tracking relation, authored by both — de-conflicted below.

**The consistency pass also found a defect in `requirements-authoring`'s own worked example**, recorded here rather than there because the ledger is where decisions live. The reserved-ID example's acceptance criterion reads *"exactly the cap proceeds **and the binding is recorded**"* while its statement obliges only the promotion — a criterion testing an obligation its own statement does not make. The batch split that obligation into `FR-064` and `FR-065` on singularity grounds, which is what exposed it: the illustration violates the rule it illustrates.

**The safety board was convened before filing rather than after, and it blocked the batch.** `@safety-sentinel` returned **seven blocking findings** on the thirty-three authored records, and every one was repaired before anything reached this folder. Convening after filing would have put seven safety defects into the corpus as records awaiting signature. The findings are recorded because the class matters more than the instances:

- **The batch completed a path to the confident access class around a hole it did not create.** `SPECIFICATION.md § Directly road-accessible zones` states two further conditions on the direct class in one sentence — the road-class and speed-limit exclusions, and level compatibility. The batch authored the second as `FR-070` and dropped the first. Alone that is an omission; with `FR-067` obliging every candidate to land in exactly one of four classes, and `FR-070`'s third criterion affirmatively declining to refuse the direct class, it became a reachable path: a German autobahn zone tagged `maxspeed=none`, six metres from a drivable edge, levels compatible, classified directly road-accessible and handed to a driver as a stop. **The repair is `FR-089`**, which gates the class and cites `SPECIFICATION.md § Enforceable exclusions` for content it does not restate. The exclusion machinery itself is a debt below.
- **A confident class was reachable with no elevation profile at all, without touching the fallback.** `CalculationSpecification.md § Elevation-aware walking time` supplies a third estimate, its flat per-segment building block. Priced from a routed path length with no profile, a candidate is not straight-line-priced, so the fallback prohibition never fires; the path length is the path length, so `FR-074` passes; connectivity is certain, so the confidence gate does not close. `FR-083` was re-keyed to require an obtained profile **positively** rather than forbidding one named estimate — the enumeration-of-forbidden-estimates form always has one more estimate in it.
- **The confidence gate was narrower than its source.** `SPECIFICATION.md § Terrain confidence` enumerates six lower-confidence inputs disjunctively; `FR-082` conjoined two of them. Because no confidence scale exists anywhere, the criteria **are** the operative definition, so four of the six would never have lowered confidence in anything tested — missing barrier information most damagingly, being the same evidence class `FR-070` tests on the other branch.
- **`FR-061` composed the total for the stated limit and not for the ceiling.** `FR-045` forbids offering an alternative above the ceiling and nothing composed that quantity, while `FR-062` obliges both halves to be retained — so the implementer had two figures to choose from on the one check that may never be wrong. Repaired on `FR-061`, whose statement now tests both limits over the same composed sum, on `FR-053`'s precedent. **`FR-045` was not touched**, and did not need to be.
- **Nothing obliged the system to identify a stopping position**, though eight records pivot on one. An implementation snapping the zone coordinate to the nearest drivable edge manufactures one for every candidate in the extract, which defeats `FR-076`'s exclusion **by the definition of its own input**. Worse, `FR-086` routes the driving cost through a stopping location while `FR-074` measures walking from a stopping position, with nothing binding them to one object — two halves of one stop, every stop still yielding a plausible number. `FR-092` and `FR-093` are the repair, split because they fail independently.
- **Two further findings and their repairs.** The implausible-gradient threshold, which has no value in any document, could be wired to the user's *maximum acceptable path gradient* by any implementer looking for a value — `FR-076` now states the two are distinct objects and `FR-077` gains a criterion in that dimension. And nothing refused operation with that threshold unset, leaving the steep limb of `FR-076` dead on first deployment while every one of its tests passed on the other two limbs — `FR-091` refuses it.

**One relayed finding was wrong, and the author was right to refuse it.** `FR-070`'s and `FR-075`'s `Volatility` cited `Architecture.md § D4` for the attributes they read, and the board judged the citation misplaced. D4 does list barriers, `layer`, access restrictions and `maxspeed` by name; **D4 is correct and was not moved.** The `settled` value was still wrong, for a reason neither the board nor `@requirements-engineer` reached: `Architecture.md § Still owed by this document` records the OSM-derived feature tables as owed, undesigned and *"a safety surface owed its own design and its own review"*. Three records now read `open-question` against that section. Recorded because a reviewer's finding accepted without checking is worth as little as one refused without arguing.

**`Volatility` was mandatory for the first time here, and it earned itself.** Across the batch's thirty-eight records it reads `settled` on twenty-one, `proposed-constant` on nine, `open-question` on eight; `FR-008`'s gain takes the corpus to thirty-nine records carrying the field, twenty-two of them `settled`. Counted from the filed corpus rather than transcribed from the authoring reports, because the first figure written into this paragraph was neither. The proposed-constant records name the corridor half-width with its floor and cap, the promotion cap, and the direct-access tolerance; the open-question records name the missing implausible-gradient constant, the missing access-confidence scale, and the undesigned feature tables above. **Two of the seven blocking findings landed on records the field had already marked**, which is the field working in the direction it was meant to rather than a claim that it found them: `FR-076` carried `open-question` naming a constant with no value, and the finding was that nothing refused running without it. The field is where a reader now finds that exposure without reading nine `Rationale` fields.

**The confidence relation was authored by both lanes and filed on the non-functional side.** `FR-081`'s second criterion and `NFR-006` asserted the same thing — that confidence falls as the evidence thins. It is `NFR-006`'s on the register's own wording, which puts confidence falling with thin data under `Coverage and data quality` by name, and on batch 3's precedent that a quality goes to the non-functional lane. `FR-081` keeps the **assignment**, which nothing else obliges and which a system satisfying `NFR-006`'s ordering can still fail by computing a level it never records. That split is `FR-024` / `NFR-002`, one confidence dimension over.

**`FR-008` was amended, and it is the second signed record this corpus has edited after signature.** On the Owner's ruling of 7 August 2026 that the user's stated limit bounds an alternative's **total** additional time against the fastest conventional route — the general-route deviation and the Turf figure together. **The obligation did not move**: the statement, title, `Category`, `Source`, `Priority`, `Verification`, `Acceptance` and `Status` are untouched, and each general route stays its own baseline for the Turf figure exactly as the record already said. What moved is `Risk`, which had claimed the additional-time target and the ceiling rest on *this* record's quantity. After the ruling they rest on `FR-061`'s, which is built from this one — so the clause was true when written, false after the ruling, and nothing in the record would have told a reader so. `Rationale` gained the ruling and the pointer. The record also **gained `Volatility` on the opportunistic transition**, this being its first edit since the field existed; that gain was missed on the amendment and caught by `@requirements-librarian`'s conformance sweep.

**Old-form citation counts, corrected in the same pass that made them stale.** `objective-selection-and-ranking` and `outbound-rate-compliance` migrated earlier today and both now hold **zero**, so they drop out of the list: `turf-data-integration` 10, `cost-and-time-composition` 6, `zone-data-fidelity` 4, `runtime-and-deployment-shape` 4, `journey-definition` 1, `value-model` 1 — **26 in total**, down from 33. Recounted line-break-aware, which is now the only admissible way to count them: the split citation in `runtime-and-deployment-shape` is invisible to a per-line pattern and three earlier passes counted past it.

**`cost-and-time-composition.md` became eligible for the migration on this pass and was declined for the third time, and the reason is a new one.** The trigger was `FR-008`'s amendment, which is a content edit and the ordinary disqualifier. It was then eligible *again* on the librarian's filing pass, which is not — but converting it there would have moved its count in this ledger, in a file the filing commission was told not to touch, and a conversion leaving the ledger asserting 6 is the exact drift the converted-file list exists to prevent. **The named moment is the next librarian-only edit to that file that does not also move a count**, which is the story pass after sign-off.

**Owed by batch 5, and deliberately not authored.** Each with its home named.

- **The enforceable exclusions themselves**, owed to a batch scoped to `SPECIFICATION.md § Enforceable exclusions`. `FR-089` gates the *direct* class on them and no more. Four rules have no record anywhere: unknown-treated-as-too-fast with its tagging cases, the highest-applicable-value rule for a multi-valued limit, the prohibition on resolving a jurisdictional value against national defaults, and the private-or-access-restricted **area** exclusion — `FR-075` binds the *way* and says so. **A fifth is the one to schedule first**: that section forbids proposing a stop on an excluded road on **either** branch, so a park-and-walk stopping position on a motorway hard shoulder is refused today by nothing at all.
- **A stop whose legality could not be established must be labelled as such**, owed to `SPECIFICATION.md § Requirements the data cannot verify`. `FR-085`'s antecedent turns on *"a legal stopping location"* with nothing in the corpus defining what establishes legality, and read as *not marked restricted* that is unknown upgraded to permitted by defaulting.
- **The general-route usability score.** `SPECIFICATION.md § General route alternatives` asks the optimizer to score each general route on ordinary travel characteristics and Turf potential, and no such model exists in `CalculationSpecification.md`. `FR-060` takes the testable residue — the fastest route is not the sole foundation — and the obligation itself stands unmet with no model and no home. Inventing one here would have created a model with no home.
- **A scale for access confidence.** Four records rest on the enumeration under `SPECIFICATION.md § Terrain confidence` standing as a working default. Unlike the gradient threshold and the uncertain-stop bound, this one is **not** recorded as an owed constant in either document's open questions, and it should be.
- **What an uncertain zone's stated reason must name**, owed to the batch scoped to `SPECIFICATION.md § Route review and zone confirmation` — specifically that a candidate whose gradient could never be evaluated says so, since the reserve pool offers exactly that candidate to a user who has just rejected something.
- **"Every stop is a stop"**, the sixth enforceable exclusion, which has no record. `FR-009` covers its cost half and no harm scenario survives that, which is why it is a debt rather than a finding.
- **Estimated value before evaluation.** `CalculationSpecification.md § Bounding the candidate set` scores corridor members on straight-line proximity *and estimated value* to select the promoted set, and nothing states what a value estimate is for a zone that has not been evaluated. `FR-064` avoids the question by binding the count alone.

**The second-hole debt is narrowed and not closed, and the arithmetic is worth stating.** `FR-058` obliges more than one general road route where more than one viable road route exists, and `FR-060` obliges at least one alternative built on a route other than the fastest **and** the fastest to remain among them — which is two journey alternatives, on that condition. The residue is the journey with one viable road route, where several Turf strategies on one road is exactly the plural set `FR-013` was written for and nothing yet produces it. `FR-058` is `MUST` and `FR-060` is `SHOULD`, which is a **sequencing hazard rather than a priority error**: `FR-058` has no consumer at all until `FR-060` is built, so a board promoting by priority alone builds three general routes and uses one. Raising `FR-060` was weighed and declined — its failure costs result variety and puts no driver anywhere, which is the test that placed `FR-041`, `FR-051`, `FR-052`, `FR-054`, `FR-057` and `FR-071` — and the pair is flagged to be scheduled together instead.

**The batch is unsigned, and two conditions attach to its signature.** First, an upstream question for the Owner: `CalculationSpecification.md § Flat-distance fallback`'s opening clause grants the fallback where *"no connected path **or** elevation profile can be obtained"*, which the flowchart under `CalculationSpecification.md § Stop time` contradicts. The proposed default is the flowchart's — **an absent or disconnected path is excluded, and the fallback exists only where a connected path is identified and its profile cannot be obtained** — because `SPECIFICATION.md § Enforceable exclusions` makes absent and disconnected a hard rule, and a hard rule cannot be satisfied by pricing the candidate anyway. `FR-074` and `FR-083` are filed against that default; if the Owner rules the other way, both widen. **This is a document repair rather than a requirement choice**, and it is not `@requirements-engineer`'s to make. Second, `@safety-sentinel` has not seen the five records added by its own remand, and re-convening it on `FR-089` … `FR-093` is a condition of signature rather than a courtesy.

## How this folder works

- **IDs are immutable.** Never reused, never renumbered, never recycled after deletion. Whoever issues the next one takes it from the ledger above and nowhere else.
- **`INDEX.md` carries a row for every ID ever issued**, including retired ones. A retired requirement keeps its record, in place and in ID order, reading `Status: retired`; it is never deleted and never moved. There are no tombstones today because no ID has ever been retired — `FR-001` … `FR-057` and `NFR-001` … `NFR-005` are each contiguous with no gaps.
- **A retired record's `Resolved-by` freezes at its last value.** Retirement removes the requirement from the *Requirement → story* table in `TRACEABILITY.md`, so the link's single home disappears with it — but the field on the record and the column in `INDEX.md` keep whatever they last read rather than reverting to `—`. They are then the only surviving record of what was built for that requirement, and blanking them would erase it. A frozen value is not stale and is not a finding.
- **Records sit in ID ascending order inside their file, and that order never changes** — not on retirement, not on re-prioritization. A file's order is therefore a fact about IDs alone, and a diff never moves a record it did not touch.
- **The record is authoritative; every index is derived.** Where a row in `INDEX.md` and the record in a category file disagree, the record is right and the row is stale — a librarian finding, fixed in the index, never fixed by editing the record to match.
- **Records are filed verbatim inside fenced blocks**, preserving the field alignment of the canonical record in `requirements-authoring`. The librarian moves, groups, indexes and cross-links records; it never re-words one. Content questions go to `@requirements-engineer`.
- **Category files are named as the kebab-cased `Category` field**, verbatim and unnumbered, so a record names its file and a file names its category with nothing to look up, and adding a category never renames one.
- **A `Category` never spans both kinds, and the `Kind` column holds one value.** Raised as a finding on 3 August 2026 by `@requirements-librarian` and ruled the same day. `Turf data integration` had been registered `FR + NFR`, producing a file that held ten FRs and then `NFR-001` … `NFR-002`, and `requirements-authoring § Corpus layout` cannot be satisfied whole by such a category: it states in one breath that a file is named as its `Category` verbatim and that **FRs and NFRs share the folder, never a file**. The batch filed under the naming rule — the one traceability rests on, since a record names its file with nothing to look up — and marked the file explicitly non-precedent, which is why the ruling cost a re-file rather than a migration. `@requirements-engineer` proposed amending the skill so that one category may hold both kinds; **the Owner kept the skill as written and split the category**, on the ground that a file's kind then stays trivially checkable and the librarian's conformance sweep stays mechanical. `NFR-001` and `NFR-002` moved to `Outbound rate compliance` and `Data currency and confidence`, registered above with their reasoning.
